# 2026-09-11

## Bối cảnh

Người dùng gia hạn chỉ thị `/goal` liên tục từ hôm qua: "tiếp tục update (tự suy nghĩ hướng phát
triển, lên kế hoạch chi tiết, triển khai kế hoạch đó) cho đến khi tôi bảo dừng lại". Tiếp tục
vòng lặp: chọn 1 việc cụ thể từ backlog, làm, verify, cập nhật rules, commit+push.

## Task 8: CI/CD tối thiểu (`.github/workflows/ci.yml`)

`rules/tasks/backlog.md` mục "Việc kỹ thuật chưa làm" đã ghi từ lâu: pipeline mô tả trong
`rules/workflow/deployment.md` (3 bước: dotnet build+test, frontend npm ci→lint→tsc→build, build
Docker image trên branch chính) là "mong muốn", repo chưa có `.github/workflows/` thật nào.

**Thay đổi:** `.github/workflows/ci.yml` (mới) — 3 job:
- `backend`: `actions/setup-dotnet@v4` (8.0.x) → `dotnet restore/build/test` trên
  `backend/BoardGame.sln` (Release). Đã verify CHÍNH XÁC lệnh này chạy sạch cục bộ trước khi
  commit (118/118 pass) — tránh CI đỏ ngay lần đầu vì lệnh sai đường dẫn/thiếu file sln.
- `frontend`: `actions/setup-node@v4` (Node 20, cache npm theo `package-lock.json`) → `npm ci` →
  `npm run build` (script này đã gồm `tsc && vite build`, không cần bước type-check riêng).
- `docker`: chỉ chạy khi push thẳng `master` (không chạy trên PR) và sau khi 2 job trên xanh —
  build thử `backend/BoardGame.Api/Dockerfile` + `frontend/Dockerfile` để bắt sớm lỗi Dockerfile,
  KHÔNG push image đi đâu (repo chưa có registry/secret nào cấu hình — deploy thật vẫn thủ công).

**Phạm vi có chủ đích:**
- KHÔNG có bước "lint" — `frontend/package.json` chưa có ESLint config/script (`scripts` chỉ có
  `dev`/`build`/`preview`). Thêm lint là việc riêng (cần chọn rule set, sửa lỗi lint hiện có nếu
  có), không lặng lẽ bỏ qua trong log này.
- KHÔNG cache NuGet packages — dự án chưa có `packages.lock.json` (cần bật
  `RestorePackagesWithLockFile` để có cache key ổn định, đó là thay đổi riêng). Từng thấy restore
  cục bộ chậm/timeout tải `Microsoft.CodeAnalysis.CSharp` (xem log 2026-09-10) — CI có thể gặp
  tương tự, thêm cache sau nếu CI thật sự hay bị chậm/fail vì lý do này.

**Verify:** Parse YAML bằng `npx js-yaml` xác nhận cú pháp đúng cấu trúc dự định (jobs/steps/
if-condition). Chạy `dotnet test backend/BoardGame.sln --configuration Release` (đúng lệnh CI
sẽ chạy, khác với lệnh mọi khi `dotnet test` chạy từ `backend/`) — 118/118 pass, 3.1s. Không thể
verify job `docker`/thực tế chạy trên GitHub Actions từ máy này (không có runner cục bộ) — tự tin
qua việc lệnh `docker build` dùng đúng path/Dockerfile đã biết hoạt động (đúng path trong
`rules/workflow/deployment.md`).

**Verify CI thật trên GitHub** (không chỉ local): push xong, poll GitHub API
(`/repos/taido12358/BoardGames/actions/workflows/ci.yml/runs`) tới khi run đầu tiên `completed` —
`conclusion: success`. Không dùng `gh` CLI (không cài trong môi trường này), dùng `curl` gọi
REST API trực tiếp (repo public, không cần token cho endpoint đọc run list/status).

## Task 9: Thêm ESLint cho frontend

Task 8 vừa ghi "chưa có bước lint" là việc riêng, không lặng lẽ bỏ qua — làm luôn trong cùng
ngày vì đây đúng là việc kỹ thuật nhỏ, độc lập, giá trị rõ (bắt lỗi sớm hơn tsc/runtime).

**Thay đổi:**
- Cài `eslint`, `typescript-eslint`, `eslint-plugin-react-hooks`, `eslint-plugin-react-refresh`,
  `globals`, `@eslint/js` (đều devDependencies, pin theo major tương thích ESLint 9).
- `frontend/eslint.config.js` (flat config) — cấu hình chuẩn đúng như `npm create vite@latest --
  template react-ts` sinh ra hiện nay: `js.configs.recommended` + `tseslint.configs.recommended`
  + rule React Hooks/React Refresh. Thêm 1 rule tuỳ chỉnh: `@typescript-eslint/no-unused-vars`
  cho phép prefix `_` (khớp quy ước "tham số bỏ qua" đã dùng rải rác trong codebase).
- `package.json`: thêm script `"lint": "eslint ."`.
- `.github/workflows/ci.yml`: thêm bước `npm run lint` vào job frontend, chạy TRƯỚC `npm run
  build` (fail nhanh nếu lint lỗi, đỡ tốn thời gian build tiếp).

**Bug thật bắt được ngay lần chạy đầu** (không phải chỉ style/nitpick): `VayBatBoard.tsx` —
3 `useMemo` (`occ`/`highlights`/`nodeById`) được gọi SAU `if (!room) return null;`, vi phạm Rules
of Hooks (ESLint báo lỗi `react-hooks/rules-of-hooks`, không phải warning). Về lý thuyết, nếu
component này từng nhận một lần render với `room=null` MÀ KHÔNG unmount trước lần render kế tiếp
có `room` khác null, React sẽ throw "Rendered more/fewer hooks than during the previous render".
Tra lại `RoomRoute.tsx` xác nhận: may mắn chưa từng crash trong thực tế vì `RoomRoute` (component
cha) tự ẩn hẳn `<VayBatBoard>` (render màn "Đang vào phòng…" thay thế) mỗi khi `room` là `null`
— nên `VayBatBoard` luôn UNMOUNT hoàn toàn giữa 2 trạng thái đó, không bao giờ tự nhận render
`room=null` sau khi đã render với `room` có giá trị. Đây vẫn là vi phạm THẬT cần sửa (fragile —
bất kỳ thay đổi nào sau này khiến cha ngừng unmount board khi `room=null` sẽ crash ngay), không
phải false positive.

**Sửa:** tính `map`/`state` dạng optional (`room?.map`/`room?.state`) TRƯỚC 3 `useMemo`, để mọi
hook luôn được gọi (không điều kiện) bất kể `room` là gì; `return null` thật sự dời xuống SAU
toàn bộ hook. 2 chỗ dùng `map.nodes` bên trong hàm lồng (`nearestNode`/`onSvgPointerUp`) phải
thêm non-null assertion `map!.nodes` vì TypeScript không lan truyền narrowing của biến ngoài vào
bên trong khai báo `function` lồng (giới hạn đã biết của TS, không phải bug) — dùng đúng convention
`!` đã có sẵn trong file này cho `room!.id`.

**`npm audit`:** cài xong phát hiện 8 lỗ hổng dev-dependency có sẵn từ trước (transitive qua
`vite`/`tailwindcss`/`react-router-dom`), không phải do lint gây ra. `npm audit fix` (không
`--force`) xử lý gọn 4 (`baseline-browser-mapping`, `browserslist`, `nanoid`, `postcss`) không
đổi behavior. Còn `vite`→8 và `react-router-dom`→7 cần `--force` (breaking change) — để riêng,
ghi thành mục backlog mới "Nâng cấp dependency frontend có breaking change".

**Tests:** `npm run lint`: 0 lỗi (2 warning `react-refresh/only-export-components` chấp nhận, xem
backlog để biết lý do không sửa). `npm run build` (`tsc && vite build`): sạch. `dotnet test
backend/BoardGame.sln --configuration Release`: 118/118 pass (không đổi backend, chạy lại cho
chắc vì đây là commit chung phiên làm việc).

## Task 10: Test tích hợp Postgres thật cho RoomService (Testcontainers)

Nợ kỹ thuật cũ nhất trong backlog (ghi từ 2026-08-05, nhắc lại 2026-09-05): `RoomService` dùng
`SELECT ... FOR UPDATE` và `FOR UPDATE SKIP LOCKED` — hành vi khoá hàng thật chỉ verify được với
Postgres thật + nhiều connection đồng thời, EF Core InMemory/test luật thuần không mô phỏng được.
Docker khả dụng trong môi trường này (`docker version` xác nhận Docker Desktop 29.4.3) nên làm
ngay, đúng đề xuất cũ trong backlog ("bổ sung Testcontainers nếu làm tiếp phần này").

**Cân nhắc trước khi bắt đầu — có nên làm debug panel Bang (spec §51) thay vào đây không?** Đã
đọc header `BangRules.cs` (625 dòng, "engine lớn nhất repo, đọc comment trước khi sửa") và cân
nhắc kỹ: spec yêu cầu "switch between test players" (xem/điều khiển BẤT KỲ ghế nào bất kể đăng
nhập là ai) — đúng loại bypass đã bị vá làm lỗ hổng bảo mật 2026-08-05 ("ai cũng giả được người
khác"). Dù chỉ định bật ở Development, việc tự ý xây lại một dạng bypass danh tính (kể cả có
gate `IsDevelopment()`) là hành động có rủi ro thật nếu gate sai/lỡ tay — chọn KHÔNG tự làm việc
này mà không hỏi người dùng trước; ưu tiên trả nợ kỹ thuật Testcontainers (an toàn, giá trị rõ,
không có rủi ro bảo mật) trước. Debug panel vẫn còn trong backlog, chờ xác nhận riêng nếu cần.

**Quy trình tách `SchemaBootstrapper.cs` — CẨN TRỌNG vì đây đúng vùng code gây sự cố mất dữ liệu
2026-09-05** (một agent trước lỡ chạy `DROP TABLE` trên DB dev thật lúc thử migration ở khu vực
này). Để không lặp lại sai sót tương tự (lần này là "gõ nhầm SQL lúc tách file" thay vì "chạy
nhầm lệnh"), làm theo quy trình:
1. Dùng `sed -n '108,227p' Program.cs` xuất ĐÚNG block `string[] schemaSqls = [...]` ra file tạm
   — không gõ lại tay (rủi ro cao nhất là gõ sai 1 ký tự trong hàng chục dòng SQL có escape phức
   tạp như `'{{}}'::jsonb`, các khối `DO $mig_seats$ ... EXECUTE $sql$ ... $sql$ ... END`).
2. Ghép file mới bằng `cat header.txt sqls_body.txt footer.txt > SchemaBootstrapper.cs` — header/
   footer (class wrapper, method `ApplyAsync`) không chứa SQL nên an toàn để tự viết tay.
3. Verify lại bằng `diff` giữa nội dung SQL đã trích xuất gốc và nội dung THỰC TẾ nằm trong file
   mới (trích lại theo đúng line range) — xác nhận khớp 100% byte-for-byte trước khi đụng tiếp.
4. Sửa `Program.cs` thay khối cũ bằng 1 dòng gọi `SchemaBootstrapper.ApplyAsync(db, logger)`.
5. `dotnet build`: sạch.
6. **Smoke-test THẬT** — dựng 1 container Postgres RIÊNG, TẠM, KHÔNG liên quan gì tới Docker
   Compose dev (`docker run --rm -d --name bg-schema-smoke -p 15432:5432 postgres:16-alpine`),
   `dotnet run` backend trỏ vào container đó qua `ConnectionStrings__Postgres`, xác nhận log có
   "Schema bootstrap hoàn tất (lần 1)." + "Application started" + KHÔNG có exception nào, rồi
   `docker rm -f bg-schema-smoke` dọn sạch ngay. Việc này chứng minh SQL sau khi tách vẫn chạy
   đúng trên Postgres thật — không chỉ tin vào diff tĩnh.

**Thay đổi:**
- `Data/SchemaBootstrapper.cs` (mới) — `Sqls` (COPY NGUYÊN VẸN, verify diff) + `ApplyAsync(db,
  logger, maxAttempts=10, retryDelay=5s)` (retry logic y hệt cũ, chỉ đổi thành method tái dùng
  được thay vì top-level statement).
- `Program.cs`: xoá ~160 dòng, thay bằng gọi `SchemaBootstrapper.ApplyAsync`.
- `Platform/RoomServiceIntegrationTests.cs` (mới) — implement `IAsyncLifetime`, mỗi test tự dựng
  1 `PostgreSqlContainer` (Testcontainers) RIÊNG (đổi tốc độ lấy cô lập tuyệt đối — xUnit tạo lại
  instance class cho mỗi `[Fact]` nên `InitializeAsync`/`DisposeAsync` tự nhiên chạy theo từng
  test). Mỗi test tự dựng `AppDbContext`/`RoomService` MỚI cho mỗi "request" mô phỏng (không
  share instance giữa các cuộc gọi đồng thời — đúng vòng đời scoped thật của ASP.NET Core, tránh
  bug "DbContext không thread-safe"). 5 test, tất cả nhắm đúng vào hành vi khoá hàng:
  - `CreateRoomAsync_PersistsRoomWithOwnerSeatedAndCorrectSeatCount` — sanity check persist thật.
  - `JoinRoomAsync_ConcurrentJoinsMoreThanOpenSeats_NoTwoUsersGetSameSeat` — 5 người tranh 3 ghế
    trống cùng lúc, verify không ai bị gán trùng ghế (FOR UPDATE serialize đúng).
  - `CancelRoomAsync_ConcurrentCancelAttemptsByOwner_OnlyOneSucceeds` — huỷ đồng thời 5 lần, chỉ
    đúng 1 lần "Ok", còn lại "NotWaiting" (không có khoá thật thì có thể nhiều lần cùng "Ok").
  - `QuickMatchAsync_ConcurrentRequestsForLastOpenSeat_OnlyOneJoinsExistingRoom` — 4 người tranh
    1 ghế trống cuối, verify SKIP LOCKED khiến chỉ 1 người ghép được, số còn lại tự tạo phòng mới.
  - `ApplySeatTimeoutAsync_ConcurrentCallsOnSameSeat_OnlyFirstProducesOutcome` — gọi timeout đồng
    thời 5 lần cho cùng 1 ghế, chỉ lần đầu tạo kết quả thật (đếm riêng `GameMoves` theo `Side`
    để không nhầm với move `__room_full__` mà `JoinRoomAsync` đã ghi lúc đủ ghế).
- `BoardGame.Api.Tests.csproj`: thêm `Testcontainers.PostgreSql` 4.15.0 + pin trực tiếp
  `Microsoft.EntityFrameworkCore` 8.0.6 (xem "Vấn đề version-resolution phát hiện được" bên dưới).

**Vấn đề version-resolution phát hiện được (có sẵn từ trước, không phải do việc này gây ra)**:
build lỗi `CS1705` ngay khi file test đầu tiên dùng `DbContextOptionsBuilder<AppDbContext>`.
Nguyên nhân: `BoardGame.Api.csproj` có `Npgsql.EntityFrameworkCore.PostgreSQL` 8.0.4 VÀ
`Microsoft.EntityFrameworkCore.Design` 8.0.6 (`PrivateAssets="all"`) cùng lúc — bản thân
`BoardGame.Api.dll` compile dùng EF Core 8.0.6 (NuGet chọn version cao hơn khi tự build), nhưng
project tham chiếu tới nó qua `ProjectReference` (như Tests) chỉ thấy nhánh 8.0.4 vì nhánh 8.0.6
bị `PrivateAssets` chặn lan truyền. Đây LUÔN là vấn đề tiềm ẩn (warning `MSB3277` đã xuất hiện từ
lâu, thấy trong mọi build log trước giờ) nhưng chưa từng thành lỗi cứng vì chưa có test nào THẬT
SỰ cần bind tới kiểu `Microsoft.EntityFrameworkCore` — test tích hợp này là lần đầu. Không có bản
Npgsql 8.0.6 trong dòng 8.0.x để sửa tận gốc (kiểm bằng `dotnet list package --outdated`) — pin
trực tiếp EF Core 8.0.6 trong Tests project là cách chuẩn cho tình huống "assembly riêng dùng
version cao hơn nhánh transitive nhìn thấy được" này.

**Tests:** `dotnet build`: sạch. `dotnet test backend/BoardGame.sln --configuration Release`:
123/123 pass (118 cũ + 5 mới), 25.6 giây. Xác nhận `docker ps -a` sau khi chạy xong không còn
container/volume nào sót (Testcontainers tự dọn qua reaper "Ryuk"). Máy này có sẵn container của
dự án KHÁC đang chạy (`app-web`/`app-api`/`app-postgres-1`/`healthflow-*`) — xác nhận không đụng
tới, chỉ quan sát bằng `docker ps -a`, không dừng/xoá bất cứ gì không phải của mình.

**CI**: 5 test mới cần Docker khả dụng lúc `dotnet test` chạy — GitHub Actions `ubuntu-latest` có
Docker cài sẵn mặc định nên không cần sửa `.github/workflows/ci.yml`. Đã push và verify chạy
thật thành công trên Actions (poll qua GitHub API tới khi `conclusion: success`).

## Task 11: Game thứ ba — Ô Ăn Quan (trò chơi dân gian Việt Nam)

Với 2 game hiện có (VayBat, Bang) đã "hoàn thiện" tương đối đầy đủ sau các task trước trong đợt
này (chat, rematch, unit test, UI đầy đủ), phần còn lại rõ ràng nhất từ chỉ thị gốc ("thêm game")
là làm game thứ ba. Đã cân nhắc dùng asset 12-cung-hoàng-đạo có sẵn (`frontend/public/assets/
games/zodiac/`) nhưng bộ asset đó ngụ ý một game phức tạp (shop/equipment/dice/crates) — rủi ro
làm vội một game lớn, chất lượng thấp. Chọn "Ô Ăn Quan" thay vào đó: trò chơi dân gian Việt Nam
quen thuộc, luật rõ ràng (không hidden info, không may rủi), độ phức tạp kỹ thuật gần với VayBat
hơn là Bang — phù hợp làm tốt trong 1 đợt, và có giá trị văn hoá/thẩm mỹ ăn khớp với "Vây Bắt"
(cũng là game dân gian) hơn là asset phương Tây có sẵn.

**Tra cứu luật trước khi code** (vì đây là game QUEN THUỘC với người Việt — sai luật sẽ bị nhận
ra ngay): dùng `WebSearch`/`WebFetch` tra Wikipedia tiếng Anh, Wikipedia tiếng Việt, và một trang
hướng dẫn (bomnguyen.com). Phát hiện các nguồn KHÔNG thống nhất về "ăn liên hoàn" (chain capture)
— quyết định chọn bản "chỉ ăn 1 lần mỗi lượt" (khớp mô tả chắc chắn nhất, trích dẫn nguyên văn từ
Wikipedia EN), ghi đầy đủ lý do thành ADR riêng trong `rules/history/decisions.md` vì đây là quyết
định kỹ thuật đáng nhớ (ảnh hưởng luật chơi thật, không chỉ chi tiết implementation nội bộ).

**Backend** (`Games/OAnQuan/`): `OAnQuanTypes.cs` (`GameState` — 12 ô theo vòng cố định
`[Quan0][dân P0 x5][Quan1][dân P1 x5]`, `QuanCaptured[2]` tách biệt khỏi giá trị ô hiện tại vì ô
quan có thể có lại quân nhỏ sau khi đã bị ăn quân gốc), `OAnQuanRules.cs` (luật thuần: sowing với
relay/capture/borrow/end-game), `OAnQuanEngine.cs` (adapter, không có map thật — `NewGame` trả
`MapJson = "{}"`). Đăng ký DI trong `Program.cs` như 2 engine kia.

**Bug thật tự bắt được khi viết test đầu tiên** (không phải trong engine, mà trong CHÍNH bộ
test): các test ban đầu dựng `pits = new int[12]` (mảng trần, mặc định toàn 0) rồi chỉ set vài ô
liên quan tới kịch bản đang test — phần còn lại vô tình = 0 khiến bên kia "hết vốn" (5 ô dân
trống) NGAY SAU nước đi đang test, kích hoạt luật vay/kết thúc ván NGOÀI Ý MUỐN, làm sai lệch
hàng loạt assertion (`Scores` khác giá trị mong đợi, `Winner` bị set khi lẽ ra phải `null`...).
Đã tự phát hiện qua truy vết logic thủ công (không phải chạy test rồi mới thấy sai — cách này an
toàn hơn nhưng chậm hơn) trước khi build/run lần đầu. Sửa bằng helper `FullBoard(...)` luôn khởi
tạo từ bàn ĐẦY ĐỦ (như `CreateState()`) rồi mới áp override cho từng test — ghi thành cảnh báo
hẳn hoi ở đầu file test để không ai lặp lại sai sót này khi thêm test mới.

**Frontend** (`games/oanquan/`): `types.ts` (khớp JSON backend), `metadata.ts` (thẻ + hướng dẫn,
accent mới `"folk"` thêm vào `gameLibraryTypes.ts`/`GameCard.tsx` — màu xanh lá/nâu đất khác với
`"western"` (Bang)/`"graph"` (VayBat)), `OAnQuanBoard.tsx` (bàn 12 ô dạng lưới CSS Grid 7 cột x 2
hàng, ô Quan span 2 hàng ở 2 đầu — đúng bố cục vật lý thật của bàn Ô Ăn Quan, không phải chỉ 1
hàng 12 ô cho gọn). Chạm 1 ô dân của mình để chọn, hiện 2 nút "← Trái"/"Phải →" — ánh xạ sang
"cw"/"ccw" thật cần đảo chiều giữa 2 hàng (hàng P1 xếp ngược theo đúng thứ tự vòng quanh bàn nên
"cw" logic tương ứng "trái" trực quan ở hàng đó, ngược với hàng P0) — xử lý trong hàm
`directionFor(side, arrow)`. Đăng ký vào `gameRegistry.ts`, thêm case vào `RoomRoute.tsx`. Không
có `CreateOptions.tsx` (không có tuỳ chọn tạo phòng — bàn cờ luôn cố định).

**Tests:** `OAnQuanRulesTests.cs` (18 test, phủ: khởi tạo, mọi nhánh `ValidMove`, rải xuyên qua ô
có sẵn quân không kích hoạt gì (chỉ landing pit mới được xét), relay dừng đúng lúc rơi vào ô
quan, ăn quân ô trống-rồi-đầy, không ăn khi ô sau cũng trống, ăn quan (set flag đúng), cả 2 chiều
cw/ccw, kết thúc ván khi ăn hết 2 quan (thắng/hoà), luật hết vốn có đủ/không đủ điểm để vay).
`dotnet build`/`dotnet test backend/BoardGame.sln --configuration Release`: 141/141 pass (123 cũ
+ 18 mới). `npm run lint`: 0 lỗi. `npm run build` (`tsc && vite build`): sạch.

**CHƯA live-test qua Chrome/Docker Compose** — khác quyết định trước đó cho các UI tweak nhỏ
trên game ĐÃ CÓ (người dùng xác nhận không cần), đây là game HOÀN TOÀN MỚI nên rủi ro cao hơn.
Không tự ý bật Docker Compose + đăng nhập OTP ngay vì việc đó cần người dùng cung cấp mã OTP thật
(không thể tự động hoá hoàn toàn) — ghi rõ thành việc ưu tiên cao trong `rules/tasks/current.md`
thay vì tự ý làm gián đoạn để xin mã. Rủi ro tồn dư chủ yếu ở tầng tích hợp (JSON shape giữa
frontend/backend, UI click) — luật lõi đã có 18 test rất kỹ; pattern adapter `IGameEngine` đã
được VayBat/Bang chứng minh hoạt động đúng qua Platform nên rủi ro tích hợp thấp hơn rủi ro luật.

## Task 12: Nâng cấp `react-router-dom` 6 → 7

Mục cuối cùng còn "chưa làm" trong backlog kỹ thuật của đợt này. Đã tra cứu trước khi làm (không
đoán): React Router v7 hợp nhất `react-router`/`react-router-dom` thành 1 gói, NHƯNG
`react-router-dom` v7 vẫn được publish làm gói tương thích ngược, re-export nguyên vẹn
`BrowserRouter`/`Routes`/`Route`/`Link`/`useNavigate`/`useParams`/`useSearchParams`/`Navigate` —
đúng tập API duy nhất dự án đang dùng (không đụng gì tới "Data Router"/loader/action mới của v7).

**Thay đổi:** `npm install react-router-dom@^7` — KHÔNG sửa dòng code nào khác. `tsc` biên dịch
sạch xác nhận mọi export vẫn khớp type (nếu v7 đổi/xoá export nào đang dùng, `tsc` đã báo lỗi
ngay). `npm audit`: giảm còn đúng 1 lỗ hổng (`vite`/`esbuild`, cố ý để riêng — xem backlog).

**Tests:** `npm run build` (`tsc && vite build`), `npm run lint`: sạch. `dotnet test`: 141/141
pass (không đổi backend, chạy lại cho chắc). CHƯA live-test qua Chrome/Docker Compose — cùng lý
do các việc UI khác trong đợt này (routing cơ bản, rủi ro thấp, đã verify qua build/lint).

## Task 13: Vitest cho frontend — trả nợ "frontend không có test tự động nào"

Với backend giờ đã 141 test (kể cả integration Testcontainers) mà frontend vẫn 0 test tự động
dù đã khá phức tạp (3 board game, chat, rematch, admin, auth) — mất cân đối rõ rệt. Đây là mục
"tự nghĩ ra" (không có trong backlog cũ) nhưng khớp tinh thần "tự suy nghĩ hướng phát triển".

**Thay đổi:**
- Cài `vitest` + `jsdom` (devDependencies). Cấu hình trong `vite.config.ts` mục `test`
  (`environment: "jsdom"`, `setupFiles`) — không tạo file `vitest.config.ts` riêng, dùng chung
  `defineConfig` của Vite (cách chuẩn khi không cần tách biệt build config/test config).
- `src/test-setup.ts` (mới) — polyfill `localStorage` thủ công bằng `Map` đơn giản. Phát hiện
  vấn đề thật: dù đã set `environment: "jsdom"`, `gameStore.ts` (đọc `localStorage` lúc load
  module) vẫn lỗi `localStorage.getItem is not a function` — jsdom bản mới không tự cấp phát
  `localStorage` hoạt động được nữa nếu thiếu flag thử nghiệm `--localstorage-file` của Node;
  polyfill thủ công tránh phụ thuộc vào hành vi cụ thể của phiên bản jsdom/Node.
- Không dùng `globals: true` của Vitest (biến toàn cục `describe`/`it`/`expect`) — import tường
  minh từ `"vitest"` trong mỗi file, tránh phải cấu hình thêm types cho tsconfig/eslint.
- 3 file test (18 test): `games/vaybat/types.test.ts` (6 — `side`/`buildAdjacency`/`occupancy`/
  `legalMoves`), `games/oanquan/types.test.ts` (4 — hằng số layout/`isQuanPit`/`ownerOf`/
  `otherSide`), `platform/gameStore.test.ts` (8 — đặc biệt: test lại đúng invariant
  "KHÔNG bao giờ hạ `isMine` true→false chỉ vì broadcast đến sau" đã ghi trong comment gốc của
  `upsertRoom`, giới hạn 200 tin nhắn chat, `setPlayerName` persist localStorage).
- Thêm script `npm run test` (`vitest run`, chế độ 1 lần cho CI — không phải watch mode).
- Wire vào `.github/workflows/ci.yml` — chạy sau `lint`, trước `build` (fail nhanh).

**Phạm vi có chủ đích**: chỉ test logic thuần (helper hiển thị/gợi ý + store), CHƯA test
component React nào — cần thêm React Testing Library (môi trường `jsdom` đã sẵn sàng, chỉ chưa
cài/viết). Ghi rõ là nợ kỹ thuật trong `rules/coding/testing.md`/backlog, không lặng lẽ dừng ở
mức "có test rồi là đủ".

**`npm audit`**: cài `vitest`/`jsdom` không phát sinh lỗ hổng MỚI về bản chất — vẫn cùng gốc
`esbuild`/`vite` đã biết và cố ý để riêng (vitest 2.x tự kéo theo 1 bản `vite-node` nội bộ dùng
chung gốc đó). Số lượng hiển thị tăng (2→5) nhưng đều quy về 1 quyết định đã có sẵn.

**Tests:** `npm run test`: 18/18 pass. `npm run lint`: 0 lỗi (không phát sinh lỗi mới từ file
test). `npm run build`: sạch (test file không lọt vào bundle production — xác nhận qua dist size
không đổi). `dotnet test`: 141/141 pass (không đổi backend, chạy lại cho chắc).

## Task 14: React Testing Library — test component đầu tiên của repo

Tiếp nối Task 13 ngay trong ngày: môi trường Vitest+jsdom đã sẵn sàng, "chưa có test component"
vừa ghi vào nợ kỹ thuật vài phút trước — làm luôn vì chi phí biên thấp (hạ tầng đã có).

**Thay đổi:**
- Cài `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`.
- `src/test-setup.ts`: thêm `import "@testing-library/jest-dom/vitest"` (mở rộng `expect()` với
  `toBeInTheDocument`/`toBeDisabled`...).
- `OAnQuanBoard.tsx`: thêm `aria-label` cho mỗi ô (`"Ô dân số 3, 5 quân"`/`"Ô Quan, 10 quân"`) —
  vừa cải thiện a11y thật, vừa là cách DUY NHẤT để test chọn đúng ô cụ thể (nhiều ô có cùng số
  quân hiển thị, ví dụ nhiều ô "5 quân", không thể phân biệt bằng text nếu chỉ hiện mỗi con số).
- `OAnQuanBoard.test.tsx` (8 test): chọn ô dân của mình → hiện nút chọn chiều → bấm đúng nút gửi
  đúng `{pitIndex, direction}`; verify chiều "Phải"/"Trái" ánh xạ ĐÚNG cw/ccw cho CẢ 2 hàng (hàng
  P1 xếp ngược quanh bàn nên ánh xạ ngược so với P0 — đúng logic `directionFor()` đã cài đặt);
  không cho chọn ô đối phương/khi chưa tới lượt (`disabled` thật, không chỉ ẩn bằng UI); hiện
  đúng thông báo thắng/hoà; khán giả (`mySide=null`) không thấy nút CHƠI LẠI dù ván đã kết thúc.

**Bug hạ tầng test tự bắt được ngay lần chạy đầu**: cả 7/8 test đều fail với lỗi "Found multiple
elements" — vì dự án không bật `globals: true` của Vitest, cơ chế tự `cleanup()` DOM sau mỗi
test của React Testing Library (dựa vào `afterEach` toàn cục mà `globals: true` mới cấp) không
tự kích hoạt, khiến DOM của test trước còn nguyên khi test sau render tiếp trong CÙNG 1 file.
Sửa bằng cách tự gọi `afterEach(() => cleanup())` trong `test-setup.ts` — hết lỗi ngay, 26/26 qua.
Đây đúng loại bug hạ tầng "nếu không viết test đầu tiên thì không bao giờ phát hiện ra", có giá
trị demo tốt cho lý do "tự suy nghĩ hướng phát triển" nên bao gồm cả cải thiện testing infra.

**Tests:** `npm run test`: 26/26 pass (18 cũ + 8 mới). `npm run lint`/`npm run build`: sạch.

## Task 15: Test component cho ChatPanel.tsx — component Platform generic

Tiếp tục đầu tư testing infra (Task 13/14) sau khi Stop hook xác nhận người dùng chưa yêu cầu
dừng — chọn `ChatPanel.tsx` vì đây là component DÙNG CHUNG cho MỌI game (không riêng game nào),
lại đơn giản/tách biệt hơn `BangBoard`/`VayBatBoard` (không cần mock nhiều state game cụ thể).

**Thay đổi:** `ChatPanel.test.tsx` (7 test) — mở/thu gọn panel, hiện đúng số lượng/nội
dung/người gửi tin nhắn, disable nút Gửi khi ô nhập rỗng/chỉ khoảng trắng, gửi tin gọi đúng
`sendChatMessage(roomId, textĐãTrim)` rồi xoá ô nhập. Mock `GameRoomHubActions` bằng
`<GameRoomHubProvider value={...}>` bọc quanh component thay vì `vi.mock` cả module — đơn giản
hơn và vẫn kiểm tra đúng contract thật giữa `ChatPanel` và context.

**Bug hạ tầng test thứ 2 tự bắt được**: 5/7 test đầu tiên fail với `scrollTo is not a function`
— jsdom KHÔNG triển khai `Element.scrollTo` (không làm layout/scroll thật), nên
`listRef.current?.scrollTo(...)` trong `ChatPanel` (tự cuộn xuống tin mới nhất) crash ngay khi
effect chạy lúc mở panel. Sửa bằng polyfill no-op MỘT LẦN trong `test-setup.ts`
(`Element.prototype.scrollTo = () => {}` nếu chưa có) — cùng chỗ, cùng tinh thần với polyfill
`localStorage` đã thêm ở Task 13, để component tương lai dùng `scrollTo`/`scrollIntoView` không
gặp lại lỗi này.

**Tests:** `npm run test`: 33/33 pass (26 cũ + 7 mới). `npm run lint`/`npm run build`: sạch.

## Task 16: Test component cho AdminPage.tsx

Tiếp tục đầu tư testing infra (Task 13/14/15). Chọn `AdminPage.tsx` vì đây là component gọi
`fetch()` thật (qua `adminStore.ts`) — khác kiểu với 2 component đã test trước đó (chỉ đọc store/
gọi hub action), nên là ví dụ mẫu tốt cho cách mock `global.fetch` theo URL.

**Thay đổi:** `AdminPage.test.tsx` (6 test) — mock `global.fetch` định tuyến theo URL (khớp
đúng path `AdminController` thật dùng: `check`/`rooms`/`stats`) thay vì mock cả module
`adminStore.ts`, để kiểm được đúng CONTRACT request/response thật giữa component-store-server
(nếu sau này đổi shape response mà quên cập nhật component, test sẽ đỏ). Phủ: đang kiểm tra
quyền (trước khi `check` trả lời), không phải admin thì KHÔNG gọi `rooms`/`stats` (verify qua
danh sách `fetch.mock.calls`), admin thấy đúng thống kê + bảng phòng, trống khi không có phòng
khớp lọc, hiện lỗi khi fetch phòng thất bại (500), bấm chip lọc gọi lại đúng query `status=...`.

**Bài học thứ 3**: 1 test fail ngay lần chạy đầu vì "Đang chờ" khớp CẢ badge trạng thái của 1
dòng bảng LẪN nút chip lọc cùng tên — chip lọc (`Object.entries(STATUS_LABEL).map(...)`) luôn
hiển thị KHÔNG PHỤ THUỘC dữ liệu đã tải, nên `getByText("Đang chờ")` mơ hồ ngay khi phòng test
cũng có status "Waiting". Sửa bằng `within(row)` giới hạn truy vấn vào đúng `<tr>` thay vì tìm
trên toàn trang — bài học chung: UI càng nhiều chỗ dùng lại cùng 1 bảng nhãn hiển thị
(`STATUS_LABEL`) thì càng dễ gặp lỗi test kiểu này, nên ưu tiên `within()`/`getByRole` có phạm
vi hẹp hơn là `getByText` toàn trang khi nhãn không chắc chắn duy nhất.

**Tests:** `npm run test`: 39/39 pass (33 cũ + 6 mới). `npm run lint`/`npm run build`: sạch.
`dotnet test`: 141/141 pass (không đổi backend). CI: verify commit ChatPanel (Task 15) chạy
thật thành công trên GitHub Actions trước khi bắt đầu task này.

## Task 16: Test component `BangBoard.tsx` (trả nợ kỹ thuật ghi ở Task 15)

**Việc:** thêm `games/bang/BangBoard.test.tsx` (React Testing Library, 8 test), tập trung vào
luồng bỏ bài khi vượt giới hạn tay bài cuối lượt (tính năng thêm ở việc #3, 2026-09-10, chưa từng
có test tự động). Đọc lại `types.ts`, `BangBoard.tsx`, `ActionBar.tsx`, `HandFan.tsx`,
`CardView.tsx`, `VictoryScreen.tsx` trước khi viết mock, để khớp đúng chữ/label thật thay vì đoán
từ trí nhớ (bắt được lệch chính tả "Huỷ" so với code thật "Hủy" ngay ở bước review trước khi chạy
test).

**Test:** bấm "KẾT THÚC LƯỢT" khi `hand.length > hp` (hp=2, tay 4 lá) vào đúng chế độ chọn bài
("chọn 2 lá để bỏ (0/2)"), chưa gửi move; chọn đúng 2 lá rồi "Xác nhận bỏ bài" gửi đúng
`{type: "END_TURN", discardCardIds: [...]}`; nút xác nhận disable khi chưa đủ; không cho chọn quá
số lá overflow; "Hủy" quay lại bình thường không gửi move nào; không vào chế độ bỏ bài khi tay
bài chưa vượt giới hạn (gửi thẳng `END_TURN` không kèm `discardCardIds`); 2 test màn thắng (có
nút "🔄 CHƠI LẠI" cho người từng ngồi ghế, không có cho khán giả).

**Bài học thứ 3 (jsdom):** chạy lần đầu tất cả 8 test đều fail với
`bottomRef.current?.scrollIntoView is not a function` — `GameLogPanel.tsx` (dùng chung
VayBat/Bang) tự cuộn xuống dòng log mới bằng `scrollIntoView`, jsdom không triển khai, giống hệt
lỗ hổng `scrollTo` đã gặp ở `ChatPanel` (Task 15). Polyfill no-op thêm vào `test-setup.ts` cùng
chỗ với `scrollTo` — sau đó 8/8 pass ngay.

**Tests:** `npm run test`: 47/47 pass (39 cũ + 8 mới). `npm run lint`/`npm run build`: sạch.
`dotnet test`: 141/141 pass (không đổi backend, chỉ đổi frontend + polyfill test).

## Task 17: Test component `VayBatBoard.tsx` (trả nốt nợ kỹ thuật test frontend)

**Việc:** thêm `games/vaybat/VayBatBoard.test.tsx` (7 test) — component khó nhất để test trong
repo vì tương tác qua API hình học SVG thật (`createSVGPoint`/`getScreenCTM`/`matrixTransform`)
mà jsdom không triển khai. Dựng bàn test tối giản 3 đỉnh thẳng hàng (0—1—2, quân Đỏ ở 0, Trắng ở
2) và mock hình học bằng identity transform (toạ độ client == toạ độ SVG) khai báo cục bộ trong
file test (không đưa vào `test-setup.ts` dùng chung vì chỉ component này cần).

**Bug hạ tầng test nặng nhất tới giờ:** test đầu tiên (chọn quân rồi tap ô đích) fail với
`makeMove` không được gọi, nhưng KHÔNG có lỗi/warning nào trong output — khó hơn hẳn 3 lần trước
(luôn có exception rõ ràng). Debug bằng cách log trực tiếp `useGameStore.getState().selected`
giữa 2 bước click → phát hiện `selected` vẫn `null` sau click đầu. Cô lập tiếp bằng 2 test thử
nghiệm tối giản: (1) xác nhận `onPointerDown` THẬT SỰ được gọi (có), (2) xác nhận `event.clientX`
sau `fireEvent.pointerDown` (undefined!). Nguyên nhân gốc: bản jsdom trong repo không có global
`PointerEvent` — `fireEvent.pointerDown` của Testing Library rơi về `Event` trần khi thiếu
constructor này, mất hẳn `clientX`/`clientY`. `toSvg()` của component nhận `undefined` thành
`NaN`, `nearestNode()` không bao giờ khớp node nào → chọn quân thất bại ÂM THẦM. Khắc phục: tự
dựng `new MouseEvent("pointerdown", {clientX, clientY, bubbles: true})` rồi `fireEvent(el,
event)` thay vì gọi `fireEvent.pointerDown` trực tiếp — React lắng nghe native event theo chuỗi
`type` nên vẫn khớp đúng `onPointerDown`.

**Bug phụ cùng lúc:** `SVGSVGElement.prototype.setPointerCapture` — jsdom bản này CÓ triển khai
(không phải thiếu như `scrollTo`/`scrollIntoView` trước đây) nhưng ném lỗi khi `pointerId`
không khớp một pointer thật đang hoạt động (điều `fireEvent` giả lập không tạo ra được). Lỗi này
chặn đứng `setSelected()` chạy tiếp vì đứng ngay sau trong cùng nhánh code của
`onSvgPointerDown`. Sửa bằng cách ghi đè HẲN thành no-op (không chỉ polyfill có điều kiện khi
thiếu như các lần trước) — bài học: kiểm tra API tồn tại (`typeof === "function"`) không đủ, một
số API tồn tại nhưng có ràng buộc runtime (pointer capture state) mà môi trường test giả lập
không thoả mãn được.

**Test:** chọn quân của mình rồi tap ô kề hợp lệ gửi đúng `{pieceId, to}`; không chọn được quân
đối phương; không đi được khi chưa tới lượt; khán giả (`mySide: null`) không chọn được quân nào;
hiện đúng thông báo thắng theo phe; ẩn nút CHƠI LẠI cho khán giả dù ván đã kết thúc; hiện đúng số
lượt Đỏ đã dùng/tối đa.

**Tests:** `npm run test`: 54/54 pass (47 cũ + 7 mới). `npm run lint`/`npm run build`: sạch.
`dotnet test`: 141/141 pass (không đổi backend). Không còn nợ kỹ thuật test component frontend —
cả 3 board game (Ô Ăn Quan, Bang, Vây Bắt) đều có test.

**Verify CI thật:** cả 3 commit (Task 16/17/18 — BangBoard test, VayBatBoard test, cache NuGet)
đều `completed`/`success` trên GitHub Actions (run `34518584349`/`34519404921`/`34519619027`,
gồm cả job `docker` build thử), không chỉ xanh cục bộ. Việc verify bị trễ ~15 phút do tự làm
cạn rate limit GitHub API không xác thực (60 request/giờ) khi poll quá dồn dập (mỗi 20s) — bài
học: dùng interval dài hơn (45-60s) và chỉ log khi trạng thái đổi cho các lần poll CI sau này.

## Task 19: Test riêng cho `GameMapper` (logic tính MySide/IsMine)

**Bối cảnh:** sau khi xong việc 16-18, backlog không còn mục nào an toàn/không mơ hồ để tự làm
tiếp — 3 mục còn lại đều CẦN người dùng (live-test Ô Ăn Quan cần tài khoản Gmail thứ 2, debug
panel Bang + thao tác admin phá huỷ cần xác nhận trước) hoặc mơ hồ tới mức tự đoán rủi ro cao
(game thứ tư dùng asset zodiac — bộ asset gồm map/dice/shop card/equipment/crate/cart gợi ý một
game buôn bán+di chuyển khá phức tạp, không có sẵn luật rõ ràng như Ô Ăn Quan để tra cứu, tự
thiết kế luật từ đầu có thể tốn nhiều công mà không đúng ý người dùng — CHỦ Ý CHƯA làm, không
phải bỏ sót). Rà lại code tìm việc an toàn khác: xét test hoá `GameHub.cs` nhưng cần mock
`IHubCallerClients`/`Context`/`Groups` của SignalR và động vào field `static` private
`_connections` — rủi ro/công sức cao hơn giá trị, bỏ qua. Tìm ra `GameMapper` (trong `RoomDto.cs`)
là logic thuần (không DB/SignalR) tương tự tinh thần "rẻ, giá trị cao" của engine unit test,
nhưng CHƯA có test trực tiếp — chỉ được exercise gián tiếp qua `RoomServiceIntegrationTests`.

**Việc:** thêm `Platform/GameMapperTests.cs` (11 test): `MySideOf` (không caller/caller không
ngồi ghế nào → null; ngồi ghế 0/1 game 2 người → đúng "RED"/"WHITE" qua `VayBatEngine`; ngồi ghế
2 game N người → đúng "P2" qua `BangEngine`, dùng engine thật thay vì fake, đúng cách
`RoomServiceIntegrationTests` đã làm); `ToDto` (chủ phòng → `IsMine=true`, người khác → `false`,
khán giả → `MySide=null`); `ToSummaryDto`; và 2 test phòng thủ dữ liệu hỏng cho `SeatCodec` (qua
`GameMapper.SeatDtosOf`) — `SeatsJson` rỗng (`"[]"`) hoặc không parse được (`"{not valid json"`)
đều phải trả về ghế trống thay vì throw, vì đây chính là lớp phòng thủ "dữ liệu cũ/thiếu không
được làm sập request" mà code đã cố tình viết nhưng chưa từng bị test buộc phải giữ đúng.

**Tests:** `dotnet test`: 152/152 pass (141 cũ + 11 mới). `npm run test`/`lint`/`build`: không đổi
frontend, vẫn 54/54 + sạch.

## Task 20: Game thứ tư — "Đua Xe Hoàng Đạo" (`zodiacrace`)

**Bối cảnh — vì sao đổi hướng so với Task 19's "Tiếp theo"**: Stop hook phản hồi rằng việc dừng
lại chờ input người dùng (kể cả khi lý do là "mơ hồ, cần hỏi trước") KHÔNG khớp điều kiện goal
("tự suy nghĩ hướng phát triển... triển khai... cho đến khi người dùng bảo dừng lại") — điều kiện
này trao quyền quyết định độc lập, không phải lý do để dừng chủ động tìm việc. Xem lại: thêm 1
game mới hoàn toàn tự chứa (không đụng Platform/game khác, nguyên tắc phân tầng trong
`rules/architecture/system.md`) không phải hành động phá huỷ/không đảo ngược — khác hẳn debug
panel Bang/thao tác admin phá huỷ (những việc THẬT SỰ cần hỏi trước, đã ghi rõ lý do). Quyết định
tiến hành, nhưng chọn cách giữ rủi ro "đoán sai" ở mức thấp nhất: xem ADR đầy đủ trong
`rules/history/decisions.md`.

**Việc:** thiết kế + triển khai game thứ tư `zodiacrace` — MVP đua xúc xắc tối giản, 2-6 người
chơi (ghế generic như Bang), KHÔNG cố tái hiện hệ shop/trang bị/thùng hàng mà bộ asset
`assets/games/zodiac/` gợi ý (không có spec để tra cứu đúng/sai, khác Ô Ăn Quan có nguồn dân gian
thật). Chỉ mượn tên "hoàng đạo" + icon Unicode 12 cung (♈-♓) làm biểu tượng xe, không dùng file
ảnh nào trong bộ asset đó — tiếp tục đúng quy ước CSS/icon thuần đã có từ 3 game trước.

**Backend** (`Games/ZodiacRace/`): `ZodiacRaceTypes.cs` (`MapDef` đường đua tĩnh + `GameState`
mảng `Positions`/`CratesCollected` theo seat index), `ZodiacRaceRules.cs` (luật thuần —
`ApplyRoll` nhận `Func<int> rollDice` thay vì `Random` trực tiếp để test kiểm soát chính xác kết
quả xúc xắc, không phụ thuộc seed), `ZodiacRaceEngine.cs` (adapter `IGameEngine` — `NewGame()`
cố tình CHƯA cấp phát `Positions`/`CratesCollected` vì seatCount thật chỉ chắc chắn biết ở
`OnRoomFull` (giống Bang, tránh trùng lặp logic `RoomService.ResolveSeatCount` rồi lệch mảng);
`OnSeatTimedOut` tự động đổ xúc xắc thay người mất kết nối thay vì xử thua ngay như VayBat/Bang,
vì đây thuần may rủi, không có thông tin ẩn/quyết định chiến lược nào bị "gian lận" khi tự động
hoá). Đăng ký DI trong `Program.cs`. 27 test mới (`ZodiacRaceRulesTests` 18 test luật thuần +
`ZodiacRaceEngineTests` 9 test adapter/hợp đồng JSON) — theo đúng khuôn `OAnQuanRulesTests`/
`VayBatEngineTests`.

**Frontend** (`games/zodiacrace/`): `types.ts` (helper `iconForSeat`/`seatIndexOfSide`/
`isMyTurn`), `metadata.ts` (mô tả + hướng dẫn 4 mục), `CreateOptions.tsx` (chọn 2-6 người, cùng
khuôn `BangCreateOptions`), `ZodiacRaceBoard.tsx` (màn "ĐANG CHỜ" khi `state.started=false` +
bảng người chơi + đường đua ngang cuộn được hiện vị trí từng xe/ô thùng hàng/ô đích + nút "ĐỔ XÚC
XẮC" khi tới lượt). Đăng ký: `Program.cs` (DI), `RoomRoute.tsx` (route theo `gameKey`),
`gameRegistry.ts`, accent theme mới `"zodiac"` trong `gameLibraryTypes.ts`/`GameCard.tsx`. 16
test mới (`types.test.ts` 7 + `ZodiacRaceBoard.test.tsx` 9).

**Bài học test nhỏ**: đường đua ngắn dùng trong test (10 ô) khiến số ô 5 trùng giá trị xúc xắc 5
— `getByText("5")` mơ hồ, sửa bằng `selector: "span.font-bold"` để giới hạn đúng phần tử hiển thị
xúc xắc (cùng lớp bài học `within(row)` đã gặp ở `AdminPage.test.tsx`).

**Tests:** `dotnet build`/`dotnet test`: 179/179 pass (152 cũ + 27 mới). `npx tsc --noEmit`:
sạch. `npm run test`: 70/70 pass (54 cũ + 16 mới). `npm run lint`/`npm run build`: sạch.

## Task 21: Live-test Ô Ăn Quan + Đua Xe Hoàng Đạo qua hệ thống sống thật (phát hiện quan trọng)

**Bối cảnh:** Task 20's "Tiếp theo" định để live-test lại "khi có dịp" (giả định vẫn cần nhiều
tài khoản Gmail thật, blocker ghi từ 2026-09-05). Trước khi chấp nhận tiếp tục chờ, kiểm tra lại
giả định đó có còn đúng không — đọc `docker-compose.yml`/`SmtpOtpSender.cs`/`AuthController.cs`
kỹ hơn thay vì tin lại ghi chú cũ.

**Phát hiện:** `docker-compose.yml` mặc định `Auth__DevLogOtp: true`, và `SmtpOtpSender` tự
fallback log mã OTP ra console (`docker compose logs backend`) bất cứ khi nào SMTP chưa cấu hình
đủ. Nghĩa là KHÔNG cần tài khoản Gmail thật để test nhiều "người chơi" — chỉ cần email bịa bất
kỳ. Vấn đề duy nhất: `.env` thật của máy này ĐÃ cấu hình SMTP thật (Gmail thật của người dùng),
nên phải tự ghi đè `EMAIL_PROVIDER=` (rỗng) CHỈ cho phiên `docker compose up` của mình
(`EMAIL_PROVIDER= docker compose up -d --force-recreate backend`), không sửa file `.env`.

**Việc:** khởi động Docker Compose (trước đó KHÔNG chạy — `docker compose ps` xác nhận rỗng),
ghi đè biến môi trường trên, tạo 2 tài khoản test (`zrace-p1@test.local`, `zrace-p2@test.local`)
qua đúng luồng OTP thật (đọc mã từ log, gọi `/api/auth/verify-otp` lấy JWT — giá trị cookie
`bg_auth` CHÍNH LÀ JWT thô, dùng trực tiếp làm `Authorization: Bearer` cho REST lẫn
`accessTokenFactory` cho SignalR). Viết 2 script Node nhỏ (`.cjs`, đặt ở scratchpad session,
không đụng `frontend/package.json`) dùng `@microsoft/signalr` sẵn có trong
`frontend/node_modules` để giả lập 2 client thật gọi `JoinRoom`/`MakeMove` qua đúng
`GameHub` thật, log lại từng `GameStateUpdated`.

**Kết quả — Đua Xe Hoàng Đạo:** tạo phòng qua REST xác nhận đúng shape ban đầu (`started:false`,
`positions:[]`, map `trackLength:30`/`crateTiles:[5,10,15,20,25]` khớp thiết kế). 2 client join
→ `OnRoomFull` chạy đúng (`started:true`, `positions:[0,0]`). Chơi tới khi có người thắng (16
lượt): xác nhận đúng luân phiên lượt, thu thập thùng hàng khi dừng đúng ô (+1 khi qua ô 10/25),
KHÔNG tính khi đi ngang qua, và overshoot về đích được clamp đúng (`26+6=32` → dừng ở `30`,
`winner:"P1"`) — khớp 100% với các test đơn vị đã viết ở Task 20, lần đầu xác nhận qua hệ thống
sống thật thay vì chỉ code-in-isolation.

**Kết quả — Ô Ăn Quan (chưa từng live-test từ lúc thêm 2026-09-11 trước đó):** tạo phòng xác
nhận đúng bàn khởi tạo (`pits:[10,5,5,5,5,5,10,5,5,5,5,5]`). 2 client join, chơi 3 nước đi thật
qua hub: rải ô1 dừng đúng ô quan (ô6) kết thúc lượt ngay theo luật; rải ô7 vòng qua hết bàn dừng
đúng ô quan đối phương (ô0); rải ô2 "ccw" tạo ra một chuỗi ăn quân/ăn quan thật
(`quanCaptured:[false,true]`, `scores:[14,0]`) — xác nhận cơ chế relay/ăn quan hoạt động đúng
trên dữ liệu Postgres thật, không chỉ trong bộ nhớ test.

**Dọn dẹp:** `docker compose down` để trả về đúng trạng thái ban đầu (không chạy) — không sửa
`.env`, không xoá volume (dữ liệu test còn lại vô hại). Ghi kỹ thuật đầy đủ vào
`rules/coding/testing.md` mục mới "Live-test nhiều người chơi thật mà KHÔNG cần nhiều tài khoản
Gmail thật" — áp dụng lại được cho MỌI kịch bản E2E từng bị gắn nhãn "cần tài khoản thật" (ngắt
mạng giữa ván, ghép trận nhanh đồng thời, sảnh realtime nhiều tab — vẫn chưa làm nhưng không còn
là blocker kỹ thuật, chỉ là việc ưu tiên thấp hơn).

## Task 22: Live-test 3 kịch bản E2E treo từ 2026-09-05 (ngắt mạng, ghép trận nhanh, sảnh realtime)

**Bối cảnh:** Task 21 vừa phát hiện blocker "cần tài khoản Gmail thật" không có thật. Thay vì để
3 kịch bản treo từ 2026-09-05 tiếp tục ở trạng thái "ưu tiên thấp hơn, chưa làm", áp dụng ngay kỹ
thuật vừa xác nhận hoạt động — không có lý do chính đáng để trì hoãn khi công cụ đã sẵn sàng.

**Kịch bản 1 — Ngắt mạng giữa ván (VayBat):** khởi động lại Docker Compose
(`EMAIL_PROVIDER= docker compose up -d`, ghi đè ngay từ đầu thay vì recreate sau như Task 21).
Tạo phòng VayBat, 2 client SignalR join qua hub thật. Gọi `conn.stop()` cho RED (mô phỏng rớt
mạng đột ngột, KHÔNG gọi `LeaveRoom` — khác hẳn rời phòng chủ động). Poll REST mỗi 5s: sau đúng
55s (khớp `DisconnectGracePeriod` 45s + 1 chu kỳ `ScanInterval` 10s của `SeatTimeoutService`),
phòng chuyển `Finished`, `winner: "WHITE"` — đúng 100% với `VayBatEngine.OnSeatTimedOut`. Không
live-test lại nhánh Bang (auto end-turn/auto-fail-respond) vì `BangSeatTimeoutTests.cs` đã unit
test trực tiếp hành vi đó; live-test VayBat đủ để xác nhận phần plumbing DÙNG CHUNG
(`SeatTimeoutService` quét/gọi `RoomService.ApplySeatTimeoutAsync`/broadcast) hoạt động đúng qua
hệ thống sống thật — phần khác nhau giữa các game chỉ nằm trong `OnSeatTimedOut` của từng engine,
đã có test riêng.

**Kịch bản 2 — Ghép trận nhanh 2 người đồng thời:** 2 tài khoản test, bắn đồng thời
`POST /api/games/quick-match` (`curl ... & curl ... & wait`, cùng `gameKey: "vaybat"`, chưa có
phòng chờ nào tồn tại trước đó). Kết quả: cả 2 response trả về CÙNG 1 `id` phòng (không tạo 2
phòng riêng, không double-book ghế) — người tạo phòng (P1) làm RED, người ghép vào sau (P2) làm
WHITE, phòng tự chuyển `Playing` ngay khi đủ ghế. Xác nhận qua tầng REST controller thật, bổ sung
cho guarantee `FOR UPDATE SKIP LOCKED` đã verify ở tầng service (`RoomServiceIntegrationTests`).

**Kịch bản 3 — Sảnh realtime 2 tab:** tab A gọi `SubscribeLobby` qua hub (không tạo phòng gì).
Tab B tạo phòng Ô Ăn Quan qua REST (hoàn toàn độc lập, không qua tab A). Tab A nhận đúng sự kiện
`LobbyUpdated` gần như ngay lập tức (trong vòng 1s) với đúng `id`/`gameKey`/`seats` của phòng vừa
tạo — xác nhận cơ chế broadcast nhóm SignalR `"lobby"` hoạt động đúng, không cần polling REST lại
để thấy phòng mới.

**Dọn dẹp:** `docker compose down` sau khi xong (trả lại trạng thái không chạy như trước khi bắt
đầu). Cả 3 script Node test nằm ở scratchpad session, không commit vào repo (chỉ là công cụ
kiểm chứng tạm thời, không phải test tự động chính thức — test tự động thật vẫn là xUnit/Vitest
đã có).

**Kết quả:** không còn việc nào treo từ đợt rebuild phòng/ghép trận 2026-09-05. Cập nhật
`rules/history/milestones.md` mục "2026-09-05" để phản ánh đã verify sống xong (trước đó ghi
"Chưa verify sống qua Docker Compose").

## Tiếp theo

Xem `rules/tasks/current.md`. Không còn "chưa live-test" cho bất kỳ game nào, không còn việc nào
treo từ đợt 2026-09-05. Còn lại: debug panel Bang (spec §51, CẦN hỏi người dùng trước — action
mutate state bỏ qua luật chơi), thao tác admin phá huỷ (CẦN hỏi trước), nâng cấp `vite`/`esbuild`
(để riêng, rủi ro cao hơn lợi ích thực tế).
