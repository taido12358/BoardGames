# Development Log — 2026-08-05

## Task

Thiết lập hệ thống tài liệu/rules cho dự án (`CLAUDE.md` làm bản đồ điều hướng + `rules/` làm kho tri thức chi tiết).

## Objective

Trước đó dự án có thư mục `rule/` (số ít) chứa 26 file quy tắc khá tốt và đúng thực trạng dự án, nhưng phẳng (không phân theo coding/architecture/workflow/tasks/logs/history/references) và không có task tracking / log / history. Mục tiêu: tổ chức lại theo cấu trúc `rules/` chuẩn, giữ nguyên nội dung đã đúng, bổ sung phần còn thiếu (architecture tách khỏi coding, tasks, logs, history, references).

## Changes

- Tạo `rules/README.md` — index điều hướng theo loại task (targeted loading, không đọc hết mọi lần).
- Tạo `rules/coding/` (README + general/frontend/backend/database/api/testing/security.md) — gộp nội dung từ 20 file cũ trong `rule/` (rule-code, rule-comment, rule-file, rule-typescript, rule-javascript, rule-component, rule-css, rule-color, rule-ui, rule-server(phần convention), rule-monitoring, rule-queue(phần convention), rule-cache(phần convention), rule-database, rule-api, rule-testing, rule-quality, rule-security, rule-auth, rule-permission).
- Tạo `rules/architecture/` (README + system/frontend/backend/database/infrastructure.md) — tách phần kiến trúc (tại sao/luồng dữ liệu/phân tầng) ra khỏi phần coding convention, nội dung từ rule-project, rule-server (phần phân tầng), rule-docker, rule-cache/rule-queue (phần vai trò hệ thống), cộng nội dung tổng quan từ `CLAUDE.md`/`README.md` gốc.
- Tạo `rules/workflow/` (README + development/git/debugging/deployment.md) — rule-git giữ nguyên gần như 100%, rule-deploy giữ nguyên, debugging.md là **toàn bộ catalog "Lỗi đã sửa"** chuyển từ `CLAUDE.md` sang (kèm quy trình debug 8 bước chuẩn), development.md gộp phần "Chạy local" từ CLAUDE.md/README + ghi chú xung đột port cục bộ đang thấy trong working tree.
- Tạo `rules/tasks/` (README + current.md + backlog.md) — current.md ghi nhận không có task tính năng dang dở + ghi chú 2 file đang sửa cục bộ (port); backlog.md ghi nhận game thứ hai đang được chuẩn bị (`van-de.md` chưa commit + asset `zodiac/` đã commit) làm rõ đây là suy luận từ file trong repo, cần hỏi lại người dùng về mapping theme trước khi code.
- Tạo `rules/history/` (README + milestones.md + decisions.md) — milestones rút từ git log thật (`git log --oneline`), decisions.md viết lại các quyết định kiến trúc đã có lý do rõ trong `CLAUDE.md` cũ dưới dạng ADR.
- Tạo `rules/references/important-files.md` — bản đồ file thật (đã `find` xác nhận từng đường dẫn tồn tại), không suy đoán.
- Viết lại `CLAUDE.md` thành file điều hướng ngắn (theo đúng khuôn mẫu prompt yêu cầu), trỏ sang `rules/`; nội dung chi tiết cũ (bug catalog, luồng auth, drag&drop...) đã chuyển sang các file `rules/*` tương ứng, không xoá thông tin — chỉ đổi chỗ ở.
- Xoá thư mục `rule/` (số ít) cũ sau khi xác nhận toàn bộ nội dung đã chuyển vào `rules/` — tránh hai nguồn tài liệu song song gây lệch nhau.
- Cập nhật đường dẫn `rule/rule-file.md` → `rules/coding/general.md` trong `frontend/public/assets/games/zodiac/README.md` (chỗ duy nhất ngoài `rule/` tham chiếu tới thư mục cũ).

## Files Modified

- `CLAUDE.md`
- `rules/**` (mới)
- `rule/**` (xoá)
- `frontend/public/assets/games/zodiac/README.md` (sửa 1 đường dẫn tham chiếu)

## Tests

Không có test tự động cho thay đổi thuần tài liệu. Đã kiểm tra thủ công: mọi đường dẫn nêu trong `rules/references/important-files.md` tồn tại thật trên đĩa (`find`/`ls`), git log dùng để viết `history/milestones.md` lấy trực tiếp từ `git log --oneline`.

## Result

SUCCESS

## Problems

- Xác nhận được (không phải suy đoán): dự án **chưa có test project nào** (`BoardGame.sln` chỉ có 1 project), `/health` chỉ trả tĩnh không kiểm tra hạ tầng, và **không có `.github/workflows/`** — ba khoảng cách này giữa tài liệu quy tắc (mô tả mong muốn) và code thật đã ghi rõ trong `tasks/backlog.md` kèm bằng chứng, không để lẫn với nội dung mô tả hiện trạng khác.
- `van-de.md` (spec game thứ hai) chưa commit — chỉ tham chiếu tới nó trong backlog, không copy nội dung vào `rules/` vì đây là draft của người dùng, chưa phải quyết định chốt.

## Decisions

- Gộp toàn bộ `rule/` cũ vào `rules/` mới thay vì viết lại từ đầu — nội dung cũ đã chính xác với thực trạng repo (đã đọc và đối chiếu từng file), viết lại sẽ có rủi ro bịa thông tin.
- Xoá `rule/` sau khi gộp thay vì giữ song song, để tránh tình trạng hai nơi quy tắc lệch nhau theo thời gian (một bên được cập nhật, một bên không).

## Next Step

- Khi bắt đầu việc tiếp theo (nhiều khả năng là game thứ hai "Zodiac", xem `tasks/backlog.md`), hỏi người dùng để chốt theme/nhân vật trước khi tạo `Games/<TênGame>/`.

---

# Development Log — 2026-08-05 (phiên 2)

## Task

Thực hiện `van-de.md`: thêm game thứ hai **BANG!** (hidden-role, 4-8 người) vào platform hiện có, theo đúng khuôn `IGameEngine`/`VayBat`.

## Objective

Spec (`van-de.md`, chưa commit) yêu cầu một game bài Western vai trò ẩn đầy đủ: 8 nhân vật, ~15 loại bài, khoảng cách bàn tròn, luật server-authoritative, UI 100% tiếng Việt, tích hợp vào Platform hiện có mà không phá Vây Bắt. Đã hỏi người dùng 2 câu trước khi làm: theme nhân vật (chọn Western gốc theo spec, không dùng asset zodiac có sẵn) và phạm vi (chọn full-stack theo đúng thứ tự 18 phase của spec, báo tiến độ theo phase).

## Changes

- **Platform (generic, không đặc thù Bang)**: `IGameEngine` thêm `RedactStateForViewer` (default interface method); `GameRoom` thêm `SeatCount`/`SeatsJson`; `GameHub` viết lại hoàn toàn luồng `JoinRoom`/`MakeMove` để hỗ trợ song song ghế 2-người (đường cũ, không đổi hành vi) và ghế N-người (mới) + broadcast state riêng theo từng connection thay vì một bản chung cho cả nhóm; `GamesController` tính `SeatCount`/`SeatsJson` lúc tạo phòng; `RoomDto` thêm field `SeatCount`/`Seats`; `GameJson` bật enum dạng chuỗi camelCase. Chi tiết quyết định: `rules/history/decisions.md`.
- **Backend `Games/Bang/`** (mới): `BangTypes.cs`, `BangCards.cs` (15 loại bài, ~71 lá), `BangCharacters.cs` (8 nhân vật), `BangRoles.cs` (bảng phân bố 4-8 người), `BangDeck.cs`, `BangRules.cs` (luật đầy đủ: khoảng cách, lượt, Bang!/Trượt!/Đấu súng/Người da đỏ!/Gatling/Hoảng loạn!/Cat Balou/vũ khí/trang bị, loại người chơi, thắng thua, projection ẩn thông tin), `BangEngine.cs`.
- **Test**: tạo `backend/BoardGame.Api.Tests` (xUnit, project test đầu tiên của repo) — 85 test cho `Games/Bang/` (roles, characters, deck, khoảng cách đúng ví dụ 6-người trong spec, luồng chơi, bảo vệ thông tin ẩn bằng cách soi JSON, hợp đồng camelCase enum).
- **Frontend**: `platform/types.ts` (RoomDto + seats), `gameStore.ts`/`useGameRoomHub.ts` (mySide tổng quát hoá thành `string | null`), `Lobby.tsx` (chọn số người chơi cho Bang), `GameView.tsx` (case "bang"); `games/bang/types.ts` (khớp chính xác JSON contract đã test) + `BangBoard.tsx` + `components/` (CardView, HandFan, PlayerSeat, LocalPlayerPanel, ActionBar, GameLogPanel, VictoryScreen).
- **Docs**: `README.md` (mục Game 002), `rules/architecture/{system,backend}.md`, `rules/coding/{database,api}.md`, `rules/history/{decisions,milestones}.md`, `rules/references/important-files.md`, `rules/tasks/backlog.md` (xoá mục Zodiac cũ, ghi các đơn giản hoá có chủ đích).

## Files Modified

- Platform: `Platform/Abstractions/IGameEngine.cs`, `Platform/Models/GameRoom.cs`, `Platform/GameHub.cs`, `Platform/GamesController.cs`, `Platform/RoomDto.cs`, `Platform/GameJson.cs`, `Data/AppDbContext.cs`, `Program.cs` (đăng ký `BangEngine` + `ALTER TABLE` cho `SeatCount`/`SeatsJson`).
- Mới: `backend/BoardGame.Api/Games/Bang/*.cs` (7 file), `backend/BoardGame.Api.Tests/**` (6 file test + csproj), `frontend/src/games/bang/**` (types + BangBoard + 7 component).
- Sửa: `frontend/src/platform/{types,gameStore,useGameRoomHub,Lobby}.ts(x)`, `frontend/src/components/GameView.tsx`.
- `README.md`, `rules/**` (liệt kê ở trên).

## Tests

- `dotnet build` (toàn solution, gồm project test mới): sạch, 0 warning mới, 0 error.
- `dotnet test`: **85/85 pass** (bao gồm test hợp đồng JSON enum, test bảo vệ thông tin ẩn).
- `npx tsc --noEmit` + `npm run build` (frontend): sạch, build ra `dist/` thành công.
- `docker compose up --build -d`: cả stack lên khoẻ (`healthy`), schema bootstrap chạy sạch lần đầu (bao gồm cột mới `SeatCount`/`SeatsJson`).
- **Verify sống bằng REST**: `GET /api/games/engines` trả cả `vaybat` và `bang`; tạo phòng `vaybat` (không đổi hành vi) và phòng `bang` (`seatCount: 4`, ghế generic đúng) đều thành công.
- **Verify sống bằng 4 SignalR client thật** (script Node dùng `@microsoft/signalr`, đã xoá sau khi test): 4 client join phòng Bang → ghế đủ → server tự chia vai trò/nhân vật/bài (Sheriff +1 HP, Jack rút 3 lá — đúng luật) → mỗi client CHỈ thấy bài/vai trò của chính mình (kiểm bằng cách soi chuỗi JSON, không phát hiện rò rỉ) → người chơi hiện tại thử Bang! vào mục tiêu ngoài tầm → server từ chối đúng với message "Mục tiêu nằm ngoài tầm bắn." Toàn bộ pipeline mới (ghế N-người, nước đi hệ thống, broadcast riêng theo viewer) hoạt động đúng trên hạ tầng thật, không chỉ trong unit test.

## Result

SUCCESS

## Problems

- MSB3277 (xung đột version `Microsoft.EntityFrameworkCore` 8.0.4 vs 8.0.6) xuất hiện khi build sau khi thêm project test — đây là version skew **có sẵn từ trước** giữa `Npgsql.EntityFrameworkCore.PostgreSQL` (8.0.4) và `Microsoft.EntityFrameworkCore.Design` (8.0.6) trong `BoardGame.Api.csproj`, chỉ lộ ra vì project test tham chiếu tới. Không sửa (ngoài phạm vi việc đang làm, rủi ro nếu tự ý đổi version package đang chạy ổn định) — ghi lại để biết đây không phải lỗi mới.
- `dotnet new xunit` mặc định tạo project nhắm `net10.0` (theo SDK máy) thay vì `net8.0` như phần còn lại của solution — đã sửa thủ công trong `.csproj`.

## Decisions

- Mở rộng `IGameEngine`/`GameRoom`/`GameHub` thay vì làm engine phụ/luồng riêng cho Bang — vì nhu cầu (>2 người, thông tin ẩn) là generic, mọi game tương lai cùng loại (vd game bài khác) đều cần, không nên gắn vào riêng Bang.
- Không xây kênh chat, nút "Chơi lại", hay UI chọn bài để bỏ khi vượt giới hạn tay bài — ghi rõ là đơn giản hoá có chủ đích trong `rules/tasks/backlog.md`, không lặng lẽ bỏ qua.
- Chọn theme Western gốc (không dùng asset zodiac có sẵn) theo lựa chọn của người dùng — asset zodiac vẫn còn đó cho việc khác sau này.

## Next Step

- Nếu muốn hoàn thiện thêm: UI chọn bài cụ thể khi bỏ bài cuối lượt (hiện tự động), health check thật, chat trong phòng, nút "Chơi lại", debug panel (Development only, spec §51).
- Cân nhắc dọn version skew EF Core (8.0.4 vs 8.0.6) khi có dịp — không khẩn cấp, build vẫn xanh.

---

# Development Log — 2026-08-05 (phiên 3)

## Task

Thay giao diện chọn game kiểu `<select>` bằng Thư viện trò chơi (game card grid + trang chi tiết theo tab hướng dẫn), theo yêu cầu UX/UI trong prompt của người dùng — chỉ đổi frontend, không đổi backend/Platform/SignalR/game engine.

## Objective

`platform/Lobby.tsx` dùng `<select>` phẳng để chọn game, không có URL riêng, không có hướng dẫn/luật chơi hiển thị trước khi vào ván. Cần: trang thư viện có tìm kiếm/lọc/thẻ trực quan, trang chi tiết mỗi game có hướng dẫn theo tab (tổng quan/vai trò/lá bài/nhân vật/khoảng cách…), giữ nguyên luồng tạo/vào phòng hiện có, kiến trúc generic để thêm game thứ ba không phải sửa lại `GameLibrary`.

## Changes

- Thêm `react-router-dom@^6.30` (chọn v6 thay vì v7 mới nhất — xem ADR trong `rules/history/decisions.md`).
- `App.tsx` bọc `<BrowserRouter>` + `ScrollToTop`; nới `max-w-lg` → `max-w-6xl` (khung cũ quá hẹp cho lưới thẻ desktop).
- `GameView.tsx` viết lại: giữ `useGameRoomHub()` DUY NHẤT ở gốc, cấp `joinRoom`/`makeMove`/`leaveRoom` qua `GameRoomHubContext` mới; nhánh "chưa có phòng" chuyển từ `<Lobby>` sang `<Routes>` (`/games`, `/games/:gameKey`).
- Thêm `platform/gameLibraryTypes.ts` (kiểu `GameMetadata`/`InstructionSection` generic) + `platform/gameRegistry.ts` (sổ đăng ký `gameKey -> metadata/instructions`, có fallback cho game chưa đăng ký).
- Thêm `games/vaybat/metadata.ts` + `games/bang/metadata.ts` — nội dung hướng dẫn viết lại chính xác theo luật ĐÃ CÀI ĐẶT thật (8 nhân vật/khả năng, 4 vai trò, 15 loại bài, khoảng cách bàn tròn…), không phải mô tả chung chung.
- Thêm component: `GameLibrary.tsx` (tìm kiếm + chip lọc sinh từ metadata thật + trạng thái loading/error/rỗng), `GameCard.tsx` (artwork CSS/emoji theo `accent`, không dùng ảnh có bản quyền), `GameDetails.tsx` (header 2 cột + tab hướng dẫn + tạo/vào phòng, tái dùng nguyên `gameStore.createRoom`/`fetchRooms`/`joinRoom`), `GameInstructions.tsx` (dispatcher nhiều `kind`: text/flow/roles/cards/characters/distanceDemo).
- Sửa lỗi có sẵn trong `gameStore.fetchEngines`/`fetchRooms`: không có try-catch, lỗi mạng bị nuốt im lặng — thêm `enginesLoading`/`enginesError` + xử lý lỗi đúng quy tắc dự án.
- Xoá `platform/Lobby.tsx` (đã grep xác nhận không còn nơi nào import).

## Files Modified

- Mới: `frontend/src/platform/{gameLibraryTypes,gameRegistry,GameRoomHubContext,ScrollToTop}.ts(x)`, `frontend/src/games/{vaybat,bang}/metadata.ts`, `frontend/src/components/{GameLibrary,GameCard,GameDetails,GameInstructions}.tsx`.
- Sửa: `App.tsx`, `components/GameView.tsx`, `platform/gameStore.ts`.
- Xoá: `platform/Lobby.tsx`.
- `frontend/package.json`/`package-lock.json` (thêm `react-router-dom`).

## Tests

- `npx tsc --noEmit`: sạch.
- `npm run build`: thành công (`dist/` build ra được, không lỗi).
- **Verify sống qua Chrome, trên Docker Compose thật** (không mock):
  - Thư viện tải đúng cả 2 game thật từ `/api/games/engines`, hiển thị "N phòng đang chờ" bằng dữ liệu thật từ `/api/games`.
  - Tìm kiếm ("đồ thị" → chỉ còn Vây Bắt) và bộ lọc (chip "4–8 người" → chỉ còn BANG!) hoạt động đúng.
  - Click thẻ → `/games/bang`, `/games/vaybat` — tab hướng dẫn hiện đúng nội dung, đúng tab theo game (Bang 8 tab gồm cả VAI TRÒ/LÁ BÀI/NHÂN VẬT, Vây Bắt 4 tab đơn giản hơn — chứng minh kiến trúc generic hoạt động).
  - Vào lại một phòng Vây Bắt **có sẵn từ trước** qua nút "VÀO PHÒNG" mới → vào ván thật, board render đúng, không đổi hành vi ván đấu.
  - Tạo phòng BANG! mới qua form mới → vào đúng màn hình chờ (`BangBoard`'s waiting screen) với đúng ghế đã chọn.
  - Nút back trình duyệt hoạt động tự nhiên (`/games/vaybat` → back → `/games`).
  - Không còn `<select>` nào trong luồng chọn game chính (grep xác nhận `Lobby.tsx` không còn được import ở đâu).
- Rebuild + restart container `frontend` qua `docker compose up --build -d frontend` để verify trên bản build Docker thật, không chỉ dev server.

## Result

SUCCESS

## Problems

- **Bug tự phát hiện & đã sửa qua verify sống**: metadata Vây Bắt có `"2 người"` vừa trong
  category vừa được GameLibrary tự sinh chip theo player count → 2 chip trùng nhãn "2 người".
  Fix: bỏ khỏi category (chip số người chơi đã tự đủ).
- **Bug tự phát hiện & đã sửa**: điều hướng route không tự cuộn về đầu trang (khác trang web
  thường) — thêm `ScrollToTop.tsx`.
- Công cụ trình duyệt (screenshot/type) không ổn định trong phiên này (timeout lặp lại, một số
  thao tác gõ/click không luôn kích hoạt sự kiện React ngay). Đã đối chiếu chéo bằng
  `read_console_messages` (không lỗi), accessibility tree (`aria-pressed`/`aria-selected` đúng
  trạng thái), và dispatch sự kiện DOM trực tiếp qua `javascript_tool` để xác nhận tính năng
  thật sự hoạt động (không phải app bug) trước khi kết luận — không báo cáo "đã test" dựa trên
  công cụ đang lỗi mà không xác minh lại bằng cách khác.

## Decisions

- Dùng React Context (`GameRoomHubContext`) thay vì gọi `useGameRoomHub()` ở nhiều nơi — hook
  mở kết nối SignalR trong `useEffect`, gọi 2 lần sẽ tạo 2 connection.
- Metadata/hướng dẫn hiển thị tách khỏi danh sách game thật của backend — `gameRegistry.ts` chỉ
  là lớp TRÌNH BÀY, game chưa có metadata tĩnh vẫn hiển thị được (fallback), đúng yêu cầu
  "không được tách rời khỏi engines list thật của backend".
- Không thêm ảnh artwork thật — dùng CSS gradient + emoji, nhất quán với cách BANG! xử lý bài
  (Unicode + CSS, không vi phạm bản quyền artwork gốc).

## Next Step

- Nếu có asset artwork thật sau này: chỉ cần thay phần render trong `GameCard.tsx`/`GameDetails.tsx`
  (đã tách metadata khỏi UI nên không cần đổi kiến trúc).
- Cân nhắc thêm game thứ ba để kiểm chứng thêm tính "không phải sửa GameLibrary" của kiến trúc registry.

---

# Development Log — 2026-08-05 (phiên 4)

## Task

Xoá trang demo "Hello, World" khỏi frontend theo yêu cầu người dùng.

## Changes

- `App.tsx`: bỏ tab switcher (`Tab` type, `TabButton`, nav 🎯 Game/👋 Hello) — chỉ còn `<GameView/>` render thẳng sau đăng nhập; bỏ `HelloPanel`.
- Xoá `frontend/src/hooks/useGameHub.ts`, `frontend/src/store/helloStore.ts` (chỉ được dùng bởi `HelloPanel`, grep xác nhận không còn tham chiếu nào khác — thư mục `hooks/`/`store/` rỗng nên xoá luôn).
- `index.html`: đổi `<title>` từ "BoardGame — Hello World" → "BoardGame".
- **Phạm vi có chủ đích**: chỉ xoá trang/route phía frontend. Backend demo hạ tầng ("Hello World" minh hoạ Postgres→Redis→RabbitMQ→OpenSearch→MinIO→SignalR) — `Controllers/HelloController.cs`, model `Greeting`, bảng `Greetings`, endpoint `/api/hello`, hub method `GameHub.SendHello` — **giữ nguyên**, không ai yêu cầu xoá backend.

## Tests

- `npx tsc --noEmit` + `npm run build`: sạch.
- Rebuild container `frontend`, verify sống qua Chrome: mở `http://localhost:5173` sau đăng nhập vào thẳng Thư viện trò chơi, không còn nav 2 tab, tiêu đề tab trình duyệt còn "BoardGame" (không còn "— Hello World").

## Result

SUCCESS

---

# Development Log — 2026-08-05 (phiên 5)

## Task

Theo yêu cầu "kiểm tra và đề xuất hướng làm về ghép phòng, vào phòng của các game": rà soát
`GameHub`/`GamesController`/luồng vào phòng, đề xuất hướng làm, người dùng chọn Giai đoạn 1
(bảo mật danh tính ghế + dọn phòng rác) để triển khai ngay.

## Objective

Rà soát phát hiện lỗ hổng bảo mật thật: `GameHub.JoinRoom`/`MakeMove` nhận `playerName` làm
tham số từ client và dùng thẳng để gán/khớp ghế — không có `[Authorize]` trên Hub/Controller,
dù app đã có JWT từ 2026-07-05. Bằng chứng: query DB thật cho thấy nhiều phòng `Waiting` bỏ
dở tồn tại hàng giờ không ai dọn. Mục tiêu: vá lỗ hổng danh tính + dọn phòng rác + cho phép
huỷ phòng, không phá luồng chơi hiện có của cả 2 game.

## Changes

- **Backend**: `Platform/Auth/ClaimsPrincipalExtensions.cs` (mới) — đọc user id/display name
  từ JWT, dùng chung ở `AuthController` (refactor 2 chỗ trùng lặp) và `GameHub`/`GamesController`.
- `GameRoom` thêm `RedPlayerId`/`WhitePlayerId`/`SeatUserIdsJson` (Guid — xác thực), song song
  cột tên hiển thị cũ (không đổi). Schema bootstrap `Program.cs` thêm 3 `ALTER TABLE`.
- `GameHub`: thêm `[Authorize]`; `JoinRoom`/`MakeMove` bỏ tham số `playerName`, danh tính từ
  `Context.User`; `JoinTwoSeat`/`JoinMultiSeat`/`ResolveSide`/map connection nội bộ đổi sang
  so khớp theo user id (Guid) thay vì chuỗi tên.
- `GamesController`: thêm `[Authorize]`; `Create()` lấy tên/id từ JWT thay vì `req.PlayerName`
  (đã xoá field này khỏi `CreateGameRequest`); thêm `POST /api/games/{id}/cancel`.
- `Services/StaleRoomCleanupService.cs` (mới, `BackgroundService`) — dọn phòng `Waiting` bỏ
  dở > 30 phút mỗi 5 phút, đánh dấu `Finished`. Đăng ký `AddHostedService` trong `Program.cs`.
- **Frontend**: `useGameRoomHub.ts` bỏ gửi `playerName` trong `JoinRoom`/`MakeMove`; báo lỗi
  lên UI nếu kết nối hub thất bại (vd cookie hết hạn). `gameStore.ts` bỏ gửi `playerName` khi
  tạo phòng, thêm `cancelRoom()`. `GameDetails.tsx` thêm nút "HUỶ" cho phòng của chính mình
  (gợi ý UI qua so tên hiển thị — quyền thật luôn do server kiểm theo JWT).

## Files Modified

- Mới: `backend/BoardGame.Api/Platform/Auth/ClaimsPrincipalExtensions.cs`, `backend/BoardGame.Api/Services/StaleRoomCleanupService.cs`.
- Sửa: `Platform/Models/GameRoom.cs`, `Data/AppDbContext.cs`, `Program.cs`, `Platform/GameHub.cs`, `Platform/GamesController.cs`, `Platform/RoomDto.cs`, `Platform/Auth/AuthController.cs`.
- Frontend: `platform/useGameRoomHub.ts`, `platform/gameStore.ts`, `components/GameDetails.tsx`.

## Tests

- `dotnet build`: sạch. `dotnet test`: **85/85 pass** (không có test nào đụng tới danh tính ghế trực tiếp — engine/rules không đổi contract, chỉ Platform xác thực thay đổi).
- `npx tsc --noEmit` + `npm run build`: sạch.
- **Verify sống trên Docker Compose thật** (rebuild backend + frontend):
  - `curl` không cookie tới `POST /api/games` → **401** (trước đây: 200, tạo phòng vô danh được).
  - Log khởi động backend: `StaleRoomCleanupService` tự chạy, **dọn đúng 1 phòng rác thật** ngay lần đầu.
  - Qua Chrome (đã đăng nhập): tạo phòng Vây Bắt → vào đúng ghế Đỏ với tên từ JWT → phòng hiện `🔴 taidotien12358 vs ⚪ —` chính xác.
  - Rời phòng → quay lại danh sách → phòng hiện nút "HUỶ" (đúng vì là chủ) → bấm huỷ → phòng biến mất khỏi danh sách ngay.
  - Tạo phòng Bang (4 ghế) → vào đúng ghế P0 với tên từ JWT → màn chờ hiện đúng "1/4 người chơi đã vào phòng".
  - Console trình duyệt sạch, không lỗi.

## Result

SUCCESS

## Decisions

- Tách cột "tên hiển thị" khỏi "user id xác thực" thay vì đổi hẳn kiểu dữ liệu cột cũ — không
  phá bất kỳ UI nào đang hiển thị tên, giảm thiểu rủi ro thay đổi.
- Tái dùng status `Finished` cho phòng bị huỷ/bỏ dở thay vì thêm status mới — mọi nơi đã xử
  lý "phòng kết thúc" tự động đúng, không cần rà lại từng chỗ.
- Không tự ý làm Giai đoạn 2/3 (ghép trận nhanh, danh sách phòng realtime, xử lý AFK) — người
  dùng chỉ chọn Giai đoạn 1; đã ghi rõ vào `tasks/backlog.md` để làm sau nếu muốn.

## Next Step

- Nếu muốn tiếp: Giai đoạn 2 (ghép trận nhanh + danh sách phòng realtime) hoặc Giai đoạn 3
  (xử lý mất kết nối/AFK) — chi tiết trong `rules/tasks/backlog.md`.
- Phòng tạo TRƯỚC bản fix này không có `RedPlayerId`/`SeatUserIdsJson` — nếu còn phòng
  `Playing` cũ nào sống sót qua đợt dọn, người chơi cũ sẽ không join lại được đúng ghế (id
  null). Chấp nhận được vì chỉ là dữ liệu test; không cần script migrate cho dữ liệu thật
  (chưa có).
