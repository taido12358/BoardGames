# Important Files

Mọi đường dẫn dưới đây đã xác nhận tồn tại trong repo (cập nhật 2026-09-05, rebuild cơ chế phòng/ghép trận). Nếu di chuyển/đổi tên file, cập nhật file này cùng PR.

## Application Entry Points

- Backend: `backend/BoardGame.Api/Program.cs` — DI, đăng ký engine từng game, schema bootstrap raw SQL, middleware pipeline.
- Frontend: `frontend/src/main.tsx` → `frontend/src/App.tsx`.

## Configuration

- Environment: `.env` (gitignored, local) / `.env.example` (committed, mẫu) — nạp bởi `backend/BoardGame.Api/Services/DotEnv.cs` khi chạy ngoài Docker. Có `ADMIN_EMAILS` (mới 2026-09-10 — CSV email được cấp role "Admin" lúc đăng nhập, xem `AdminController.cs`).
- Backend config: `backend/BoardGame.Api/appsettings.json`, `appsettings.Development.json`.
- Docker: `docker-compose.yml` (root), `backend/BoardGame.Api/Dockerfile`, `frontend/Dockerfile`, `.dockerignore` ở mỗi phía.
- CI: `.github/workflows/ci.yml` (mới 2026-09-11) — build+test backend, lint+build frontend trên mọi push/PR nhắm `master`, build thử (không push) 2 Docker image khi push thẳng `master`.
- Kubernetes: `k8s/*.yaml` (namespace, config, mỗi service hạ tầng, backend, frontend, ingress).
- Frontend build: `frontend/vite.config.ts` (có mục `test` cho Vitest, mới 2026-09-11), `frontend/tailwind.config.js`, `frontend/tsconfig.json`, `frontend/eslint.config.js` (mới 2026-09-11 — `npm run lint`, wire vào CI).

## Database

- Context: `backend/BoardGame.Api/Data/AppDbContext.cs`.
- Schema: **không có file migration** — toàn bộ `CREATE TABLE`/`ALTER TABLE` nằm trong `Data/SchemaBootstrapper.cs` (`Sqls` + `ApplyAsync`, tách khỏi `Program.cs` 2026-09-11 để test tái sử dụng đúng SQL thật — xem [`../architecture/database.md`](../architecture/database.md)). `Program.cs` chỉ gọi `SchemaBootstrapper.ApplyAsync(db, logger)` lúc boot.
- Entity generic: `backend/BoardGame.Api/Platform/Models/GameRoom.cs`, `GameMove.cs`, `GameRecord.cs`.
- Entity auth: `backend/BoardGame.Api/Platform/Auth/AppUser.cs`, `AuthOtp.cs`.

## Platform (generic, dùng chung mọi game)

- `backend/BoardGame.Api/Platform/Abstractions/IGameEngine.cs` — contract mọi game phải implement (nay có thêm `SideForSeat`/`OnRoomFull`/`OnSeatTimedOut`, default interface method).
- `backend/BoardGame.Api/Platform/Abstractions/GameEngineRegistry.cs`.
- `backend/BoardGame.Api/Platform/RoomService.cs` (mới, 2026-09-05) — toàn bộ logic phòng/ghế/ván (join/create/cancel/quick-match/timeout), gom về một chỗ thay cho việc `GameHub`/`GamesController` mỗi nơi tự rẽ nhánh riêng trước đây. `CreateRematchAsync` (mới 2026-09-10) — tạo phòng mới cùng `gameKey`/`seatCount` cho người TỪNG chơi ván `Finished`, không tự mời lại đối thủ (đó là việc của `GameHub.AnnounceRematch`).
- `backend/BoardGame.Api/Platform/GameHub.cs` — SignalR hub (`/hubs/game`), giờ chỉ là lớp transport gọi `RoomService`. `SendChatMessage` (mới 2026-09-10) — chat GENERIC trong phòng, chỉ broadcast qua nhóm SignalR, KHÔNG lưu DB (mất khi phòng đóng/tải lại trang); player lẫn spectator đều gọi được, chỉ cần đã `JoinRoom` đúng phòng. `AnnounceRematch` (mới 2026-09-10) — rebroadcast cho người khác còn ở phòng cũ biết có phòng chơi lại, không tự tạo phòng.
- `backend/BoardGame.Api/Platform/GamesController.cs` — REST lobby (`/api/games`, có thêm `POST /quick-match`, `POST /{id}/rematch` mới 2026-09-10), cũng chỉ gọi `RoomService`.
- `backend/BoardGame.Api/Platform/RoomDto.cs`, `GameJson.cs`, `GameMapper` (trong `RoomDto.cs`) — tính `MySide`/`IsMine` theo caller. Test riêng (mới 2026-09-11): `BoardGame.Api.Tests/Platform/GameMapperTests.cs` (11 test, logic thuần không DB — `MySideOf`/`ToDto`/`ToSummaryDto`/`SeatDtosOf` kể cả trường hợp `SeatsJson` rỗng/hỏng, trước đó chỉ được exercise gián tiếp qua `RoomServiceIntegrationTests`).
- `backend/BoardGame.Api/Platform/Models/SeatSlot.cs` (mới) — `SeatSlot` record + `SeatCodec` (mã hoá/giải mã `GameRoom.SeatsJson`).
- `backend/BoardGame.Api/Platform/Models/RoomStatus.cs` (mới) — hằng số 5 trạng thái + `IsOpen`.
- `backend/BoardGame.Api/Platform/Auth/` — `AuthController.cs`, `TokenService.cs` (JWT), `ClaimsPrincipalExtensions.cs` (đọc user id/display name/email từ `ClaimsPrincipal` — dùng ở `GameHub`/`GamesController`/`RoomService`/`AdminController` để xác thực ghế), OTP.
- `backend/BoardGame.Api/Platform/AdminController.cs` (mới, 2026-09-10) — `/api/admin/*` READ-ONLY: `check` (bất kỳ ai đăng nhập gọi được, trả `isAdmin` — frontend dùng để ẩn/hiện mục "Quản trị"), `rooms`/`stats` (`[Authorize(Roles = "Admin")]`, 403 tự động nếu không phải admin). Role "Admin" gán vào **claim JWT lúc đăng nhập** (`TokenService.CreateToken`, không phải kiểm tra config mỗi request) nếu email nằm trong env `ADMIN_EMAILS` (CSV, rỗng mặc định — xem `.env.example`) — đúng pattern ở `rules/coding/security.md` mục "Phân quyền" (role qua claim + `[Authorize(Roles=...)]`, không hard-code danh sách trong logic từng action). Đánh đổi: đổi `ADMIN_EMAILS` chỉ có hiệu lực từ lần đăng nhập MỚI. Không dùng cột DB mới cho quyền. Chủ ý không có thao tác phá huỷ (huỷ/xoá phòng) — xem chú thích đầu file.

## Games

- `backend/BoardGame.Api/Games/VayBat/` — game đầu tiên (2 người): `VayBatTypes.cs` (Map/State/Move), `VayBatRules.cs` (luật thuần), `VayBatEngine.cs` (adapter `IGameEngine`, override `SideForSeat`/`OnSeatTimedOut`).
- `backend/BoardGame.Api/Games/Bang/` — game thứ hai (4-8 người, hidden-role): `BangTypes.cs` (state/player/move/projection riêng-cho-người-xem), `BangCards.cs` (catalog + bộ bài), `BangCharacters.cs`, `BangRoles.cs` (bảng phân bố vai trò), `BangDeck.cs` (rút/xáo), `BangRules.cs` (luật thuần — engine lớn nhất trong repo, đọc comment đầu file trước khi sửa; mục "DEBUG" ở cuối file — `DebugForceDraw`/`DebugForceDamage`/`DebugForceEndTurn`, wrapper public tái dùng logic thật cho debug panel), `BangEngine.cs` (adapter `IGameEngine`, override `OnRoomFull`/`OnSeatTimedOut` + `RedactStateForViewer`), `BangDebugController.cs` (mới 2026-09-11 — debug panel §51, `/api/debug/bang/*`, gate `Debug:BangPanelEnabled` hoặc `IsDevelopment()`, xem ADR trong `../history/decisions.md`).
- `backend/BoardGame.Api/Games/OAnQuan/` (mới 2026-09-11) — game thứ ba, "Ô Ăn Quan" (trò chơi dân gian Việt Nam, 2 người): `OAnQuanTypes.cs` (`GameState` 12 ô + `OAnQuanMove`), `OAnQuanRules.cs` (luật thuần — rải quân 2 chiều, relay, ăn quân, "hết vốn", kết thúc ván; đọc comment đầu file về các chỗ đã chọn 1 biến thể luật cụ thể do nguồn dân gian có dị bản), `OAnQuanEngine.cs` (adapter `IGameEngine`, override `OnSeatTimedOut`, không có map thật — `NewGame` trả `MapJson = "{}"`).
- `backend/BoardGame.Api/Games/ZodiacRace/` (mới 2026-09-11) — game thứ tư, "Đua Xe Hoàng Đạo" (đua xúc xắc tự thiết kế, 2-6 người, KHÔNG dùng bộ asset ảnh `assets/games/zodiac/` — xem ADR trong `../history/decisions.md` để biết lý do): `ZodiacRaceTypes.cs` (`MapDef` đường đua tĩnh + `GameState` mảng `Positions`/`CratesCollected` theo seat index + `ZodiacRaceMove`, chỉ 1 loại move `"ROLL"`), `ZodiacRaceRules.cs` (luật thuần — `ApplyRoll` nhận `Func<int> rollDice` thay vì `Random` trực tiếp để test kiểm soát chính xác kết quả xúc xắc), `ZodiacRaceEngine.cs` (adapter `IGameEngine`, ghế generic như Bang — `NewGame()` cố tình CHƯA cấp phát mảng theo seat, `OnRoomFull` mới thật sự biết seatCount để tránh lệch với `RoomService.ResolveSeatCount`; `OnSeatTimedOut` tự động đổ xúc xắc thay người mất kết nối thay vì xử thua, vì không có thông tin ẩn/quyết định chiến lược nào bị "gian lận").

## Services (hạ tầng)

- `backend/BoardGame.Api/Services/RedisCacheService.cs`
- `backend/BoardGame.Api/Services/RabbitMqPublisher.cs`
- `backend/BoardGame.Api/Services/OpenSearchService.cs`
- `backend/BoardGame.Api/Services/MinioStorageService.cs`
- `backend/BoardGame.Api/Services/SmtpOtpSender.cs` — gửi OTP đăng nhập qua SMTP (MailKit).
- `backend/BoardGame.Api/Services/DotEnv.cs` — nạp `.env` khi chạy `dotnet run` ngoài Docker.
- `backend/BoardGame.Api/Services/StaleRoomCleanupService.cs` — `BackgroundService` dọn phòng `Waiting` bỏ dở quá 30 phút (đánh dấu `Abandoned`, trước 2026-09-05 là `Finished`).
- `backend/BoardGame.Api/Services/SeatTimeoutService.cs` (mới, 2026-09-05) — `BackgroundService` xử lý mất kết nối/AFK giữa ván (gọi `IGameEngine.OnSeatTimedOut` sau grace period).

## Frontend

- Platform (dùng chung mọi game): `frontend/src/platform/` — `authStore.ts`, `gameStore.ts` (có `chatMessages`/`rematchInvite`, mới 2026-09-10), `useGameRoomHub.ts`, `useLobbyHub.ts` (mới), `GameRoomHubContext.tsx`, `RoomShell.tsx` (banner/leave button dùng chung giữa các game — có thêm `RematchButton`/`RematchInviteBanner`, mới 2026-09-10), `ChatPanel.tsx` (mới 2026-09-10 — chat GENERIC, mount 1 lần ở `RoomRoute.tsx` cho mọi game), `ScrollToTop.tsx`, `types.ts`, `gameLibraryTypes.ts`, `gameRegistry.ts`.
- Điều hướng theo game: `frontend/src/components/GameView.tsx` (luôn render `<Routes>`) + `RoomRoute.tsx` (mới, route `/games/:gameKey/room/:roomId` — route trong-ván thật, thay cho rẽ nhánh theo `gameStore.room` trước đây; render board + `ChatPanel` cùng lúc).
- Thư viện trò chơi: `frontend/src/components/{GameLibrary,GameCard,GameDetails,GameInstructions}.tsx`.
- Quản trị (mới, 2026-09-10): `frontend/src/components/AdminPage.tsx` (route `/admin`, xem `GameView.tsx`) + `frontend/src/platform/adminStore.ts`. Chỉ hiện link "Quản trị" trong `App.tsx` khi `adminStore.isAdmin` — server (`AdminController`) mới là lớp chặn thật.
- Lịch sử ván đấu (mới 2026-09-11): `frontend/src/components/GameHistoryPage.tsx` (route `/history`, link "📜 Lịch sử" luôn hiện cho MỌI người đăng nhập trong `App.tsx` — không phải tính năng quản trị) — trả nợ tính năng: dùng `GamesController.Search`/`OpenSearchService.SearchGamesAsync` đã có sẵn từ đầu dự án (`GET /api/games/search?q=`) nhưng CHƯA từng có UI nào gọi tới trước đó. Debounce 300ms khi gõ tìm kiếm. Test: `GameHistoryPage.test.tsx` (6 test, dùng `vi.useFakeTimers()` để test debounce).
- Đổi tên hiển thị (mới 2026-09-11): `frontend/src/platform/DisplayNameEditor.tsx` (sửa tên tại chỗ ngay trên header `App.tsx`, thay `<span>{user.displayName}</span>` tĩnh cũ) + `authStore.updateDisplayName` — trả nợ tính năng thứ hai cùng đợt audit endpoint: `PUT /api/auth/display-name` có sẵn từ đầu dự án (`AuthController.cs`) nhưng CHƯA từng có UI gọi tới. Test: `authStore.test.ts` (`describe("updateDisplayName")`, 4 test) + `DisplayNameEditor.test.tsx` (8 test).
- Game VayBat: `frontend/src/games/vaybat/` — `types.ts`, `metadata.ts` (thẻ + hướng dẫn), `CreateOptions.tsx` (mới — UI tuỳ chọn tạo phòng, tách khỏi `GameDetails.tsx`), `VayBatBoard.tsx`.
- Game Bang: `frontend/src/games/bang/` — `types.ts`, `metadata.ts` (thẻ + hướng dẫn), `CreateOptions.tsx` (mới), `BangBoard.tsx`, `components/` (gồm `BangDebugPanel.tsx`, mới 2026-09-11 — chỉ hiện khi `GET /api/debug/bang/enabled` trả `true`, xem ADR debug panel trong `../history/decisions.md`).
- Game Ô Ăn Quan (mới 2026-09-11): `frontend/src/games/oanquan/` — `types.ts`, `metadata.ts` (thẻ + hướng dẫn), `OAnQuanBoard.tsx` (bàn 12 ô dạng lưới, chọn ô dân rồi chọn chiều rải). Không có `CreateOptions.tsx` (không có tuỳ chọn tạo phòng riêng). Accent theme mới `"folk"` thêm vào `gameLibraryTypes.ts`/`platform/artworkTheme.ts`/`GameCard.tsx`.
- Game thứ tư "Đua Xe Hoàng Đạo" (mới 2026-09-11, xem ADR trong `../history/decisions.md`): `frontend/src/games/zodiacrace/` — `types.ts` (helper `iconForSeat`/`seatIndexOfSide`/`isMyTurn`), `metadata.ts`, `CreateOptions.tsx` (chọn số người chơi 2-6, cùng khuôn `BangCreateOptions`), `ZodiacRaceBoard.tsx` (màn "ĐANG CHỜ" khi `state.started=false` + đường đua ngang cuộn được + bảng người chơi). Accent theme mới `"zodiac"` thêm vào `gameLibraryTypes.ts`/`platform/artworkTheme.ts`/`GameCard.tsx`. KHÔNG dùng file ảnh trong `public/assets/games/zodiac/`.
- `frontend/src/platform/artworkTheme.ts` (mới 2026-09-11) — `ARTWORK_BG` DÙNG CHUNG giữa `GameCard.tsx`/`GameDetails.tsx`, ép kiểu `Record<GameMetadata["accent"], string>` để TypeScript bắt buộc đủ key. Trả nợ 1 bug thật: trước đó mỗi file tự khai báo bản sao riêng (kiểu lỏng `Record<string, string>`), `GameDetails.tsx` thiếu hẳn `"folk"`/`"zodiac"` — trang chi tiết Ô Ăn Quan và Đua Xe Hoàng Đạo mất nền artwork mà không lỗi biên dịch nào báo, chỉ phát hiện khi rà lại code chủ động. Test regression: `artworkTheme.test.ts`.
- Demo "Hello World" (frontend + backend) đã xoá hoàn toàn (2026-09-10) — trước đó frontend đã xoá 2026-08-05, backend giữ lại vì "chưa ai yêu cầu"; nay dọn nốt theo yêu cầu tổng quát "xoá trang không cần thiết": `Controllers/HelloController.cs`, `Models/Greeting.cs`, bảng `Greetings` (ngừng tạo mới trong schema bootstrap, không DROP bảng cũ), hub method `GameHub.SendHello`, endpoint `/api/hello` không còn. `RedisCacheService`/`RabbitMqPublisher`/`MinioStorageService`/`OpenSearchService` vẫn còn — chỉ bỏ phần API/method/const dành riêng cho demo, phần dùng thật cho game (cache state, `PublishGameEvent`, `SaveReplayAsync`, `IndexGameAsync`/`SearchGamesAsync`) giữ nguyên.
- Asset tĩnh game chưa có code: `frontend/public/assets/games/zodiac/` — xem [`../tasks/backlog.md`](../tasks/backlog.md).

## Tests

- `backend/BoardGame.Api.Tests/` (thêm 2026-08-05) — xUnit, 218 test. `Platform/Auth/AuthApiFactory.cs` + `AuthControllerIntegrationTests.cs` + `AdminControllerIntegrationTests.cs` + `GamesControllerIntegrationTests.cs` (mới 2026-09-11, 29 test — HTTP tích hợp qua `WebApplicationFactory<Program>` + Testcontainers Postgres, khác `RoomServiceIntegrationTests` ở chỗ chạy qua ĐÚNG pipeline routing/`[Authorize]`/cookie JWT thật, không gọi thẳng service; lần đầu verify role Admin + luồng tạo/huỷ/ghép trận qua HTTP end-to-end. Cần `public partial class Program {}` marker cuối `Program.cs`. Xem bài học gotcha `ConfigureAppConfiguration` không ghi đè được config đọc eager trong `rules/coding/testing.md` mục "Test HTTP tích hợp qua WebApplicationFactory"). `Bang/*.cs`: roles, characters, deck, distance, luồng chơi, bảo vệ thông tin ẩn, hợp đồng JSON enum, `BangSeatTimeoutTests.cs` (mới 2026-09-05 — `OnSeatTimedOut`), `BangDebugTests.cs` (mới 2026-09-11, 10 test — `BangRules.DebugForceDraw`/`DebugForceDamage`/`DebugForceEndTurn`, xem ADR debug panel trong `../history/decisions.md`). `VayBat/VayBatEngineTests.cs` (2026-09-05 — `SideForSeat`/`OnSeatTimedOut`, lớp adapter) + `VayBat/VayBatRulesTests.cs` (2026-09-10, 17 test — luật thuần: kề/trống, nước đi hợp lệ, áp nước đi, thắng/thua, khởi tạo state). `OAnQuan/OAnQuanRulesTests.cs` (mới 2026-09-11, 18 test — rải quân/relay/ăn quân/ăn quan/hết vốn/kết thúc ván; đọc chú thích đầu file về lỗi thật đã tự bắt được lúc viết test đầu tiên — mảng `pits` phải khởi tạo từ bàn ĐẦY ĐỦ, không phải mảng trần toàn 0, nếu không sẽ vô tình kích hoạt luật "hết vốn" ngoài ý muốn). `ZodiacRace/ZodiacRaceRulesTests.cs` + `ZodiacRaceEngineTests.cs` (mới 2026-09-11, 27 test — di chuyển/về đích/thùng hàng/lượt chơi ở tầng luật thuần, và hợp đồng JSON/`OnRoomFull`/`OnSeatTimedOut` ở tầng adapter). `Platform/Auth/TokenServiceTests.cs` (2026-09-10, 4 test — round-trip JWT thật cho claim role "Admin", xem ADR trong `../history/decisions.md`). `Platform/RoomServiceIntegrationTests.cs` (2026-09-11, 5 test — Testcontainers, Postgres thật, verify khoá `FOR UPDATE`/`SKIP LOCKED` của `RoomService` bằng các cuộc gọi đồng thời thật; CẦN Docker khả dụng lúc `dotnet test` chạy, GitHub Actions `ubuntu-latest` có sẵn). `Platform/RoomStatusTests.cs` (mới). `Platform/GameMapperTests.cs` (mới 2026-09-11, 11 test — `MySideOf`/`ToDto`/`ToSummaryDto`/`SeatDtosOf`, logic thuần trước đó chỉ exercise gián tiếp).
- `frontend/` (mới 2026-09-11) — Vitest, 166 test, `npm run test`. `src/games/vaybat/types.test.ts` + `src/games/oanquan/types.test.ts` + `src/games/zodiacrace/types.test.ts` (helper hiển thị/gợi ý nước đi thuần của từng game). `src/platform/gameStore.test.ts` (merge `isMine` trong `upsertRoom`, giới hạn `chatMessages`, `rematchInvite`, `setPlayerName`, và mới 2026-09-11: `fetchEngines`/`fetchRooms`/`createRoom`/`cancelRoom`/`quickMatch` — mock `fetch`, trước đó chỉ được exercise gián tiếp qua mock trong `GameDetails.test.tsx`, chưa test thật logic thành công/lỗi HTTP/lỗi mạng của chính các action này). `src/games/oanquan/OAnQuanBoard.test.tsx` (8 test, React Testing Library — ví dụ mẫu test component đầu tiên của repo) + `src/platform/ChatPanel.test.tsx` (7 test) + `src/components/AdminPage.test.tsx` (6 test — mock `global.fetch` theo URL) + `src/games/bang/BangBoard.test.tsx` (8 test — luồng bỏ bài khi vượt giới hạn tay bài) + `src/games/vaybat/VayBatBoard.test.tsx` (7 test — mock hình học SVG + workaround jsdom thiếu `PointerEvent`) + `src/games/zodiacrace/ZodiacRaceBoard.test.tsx` (9 test) + `src/games/bang/components/BangDebugPanel.test.tsx` (6 test, mock `global.fetch` theo URL — cùng khuôn `AdminPage.test.tsx`) + `src/platform/artworkTheme.test.ts` (2 test — regression guard cho bug `ARTWORK_BG` thiếu key, xem mục `artworkTheme.ts` bên trên) + `src/components/GameDetails.test.tsx` (mới 2026-09-11, 16 test — trang chi tiết game trước đó CHƯA có test nào dù là 1 trong những trang dùng nhiều nhất; dùng `MemoryRouter` lần đầu trong bộ test vì component này cần `useParams`/`useNavigate`/`useSearchParams`. Test nền artwork đúng accent cho CẢ 4 game — render THẬT component nên bắt được đúng dạng lỗi đã xảy ra ở bug `ARTWORK_BG`, không chỉ kiểm tra hằng số như `artworkTheme.test.ts` — cộng tạo phòng/tìm trận nhanh/huỷ phòng/hiện đúng phòng đang chờ, và 6 test chuyển tab hướng dẫn qua Bang — game DUY NHẤT dùng đủ cả 6 `kind` của `InstructionSection`, lần đầu test tới `GameInstructions.tsx`'s `RolesSection`/`CardsSection`/`CharactersSection`/`DistanceDemoSection` vốn chưa ai test) + `src/platform/authStore.test.ts` (mới 2026-09-11, 12 test — luồng đăng nhập OTP hoàn chỉnh, chưa từng có test dù là logic quan trọng nhất; bài học nghiêm trọng về `vi.unstubAllGlobals()` revert nhầm stub `localStorage` dùng chung, xem `../coding/testing.md`) + `src/platform/LoginPage.test.tsx` (mới 2026-09-11, 12 test — trang đăng nhập, trang ĐẦU TIÊN mọi người dùng thấy, chưa từng có test) + `src/platform/RoomShell.test.tsx` (mới 2026-09-11, 26 test — mọi component "khung phòng" DÙNG CHUNG giữa cả 4 game, trước đó chỉ được exercise gián tiếp/không đủ nhánh qua từng board riêng lẻ; phủ hết `RoomStatusBanner`/`DisconnectBadge`/`ConnectionBanner`/`RoomErrorBanner`/`LeaveRoomButton`/`RematchButton`/`RematchInviteBanner`/`isOpenStatus`) + `src/platform/createOptionsAccessibility.test.tsx` (mới 2026-09-11, 3 test — regression cho bug a11y `<label>` không gắn `htmlFor` ở cả 3 form `CreateOptions.tsx`, xem mục bên dưới). Config ở `vite.config.ts` mục `test` (`environment: "jsdom"`, `setupFiles: ["./src/test-setup.ts"]` — polyfill `localStorage` + `Element.prototype.scrollTo`/`scrollIntoView` (jsdom không triển khai) + `afterEach(cleanup)` thủ công vì dự án không bật `globals: true`). Không còn component board nào thiếu test — xem [`../coding/testing.md`](../coding/testing.md).
- Chiến lược test đầy đủ (mong muốn cho mọi game): [`../coding/testing.md`](../coding/testing.md).

## Tài liệu

- `CLAUDE.md` (root) — điều hướng cho AI assistant, trỏ vào `rules/`.
- `rules/` — kho tri thức chi tiết (thư mục chứa file này).
- `README.md` (root) — giới thiệu dự án cho người mới, hướng dẫn chạy nhanh.
- `van-de.md` (root, **chưa commit**) — spec draft cho game thứ hai, xem [`../tasks/backlog.md`](../tasks/backlog.md).
