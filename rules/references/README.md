# References

Bản đồ file quan trọng trong repo — dùng để định vị nhanh khi chưa quen phần nào đó của codebase.

- [`important-files.md`](./important-files.md) — entry point, config, DB, API, frontend, hạ tầng.

File này chỉ trỏ đường dẫn; nội dung/quy ước thuộc về `coding/` và `architecture/`.
